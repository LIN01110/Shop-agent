# Demo 脚本

## 1. 项目简介

这是一个面向电商导购的多模态 RAG Agent 项目，核心目标是把 `推荐咨询` 能力升级到 `交易执行` 闭环。用户在 Android 原生客户端输入自然语言需求，后端完成：

- 对话意图理解与多轮上下文管理
- 向量检索 + 商品元数据过滤
- 生成式回答 + 商品卡片展示
- 购物车 CRUD 与端侧状态同步
- SSE 流式输出，支持渐进式展示结果

项目重点验证：

- 最小闭环是否从前端到后端完整打通
- 是否支持复杂约束、反选、对比、主动澄清等真实导购场景
- 是否能避免幻觉，商品信息来源于真实 metadata

## 2. 演示前检查

1. 后端已启动：`.
\scripts\run_server.ps1`
2. 真机已连接并执行：`adb reverse tcp:8000 tcp:8000`
3. `.env` 中按需启用：`USE_CHROMA=true`、`USE_LLM=true`、`USE_ARK_EMBEDDING=true`
4. Android App 已安装到真机并能打开对话页。

> 提示：如果未启用 `USE_LLM`，系统会退回本地模板回答；如果未启用 `USE_CHROMA`，优先使用 JSON fallback 检索。

## 3. 演示流程

### 3.1 最小闭环验证
现在单轮对话中进行测试上下文。

1. 打开 Android App。
2. 输入：`推荐一款保湿眼霜，预算250以内`
3. 期待结果：
   - AI 以流式返回文本
   - 展示带主图、标题、价格等信息的商品卡片
   - 点击商品卡片弹出本地商品详情
   - 在详情页点击 `打开链接`，进入 `/products/{product_id}` 本地详情页

### 3.2 多轮交互与反选
数据库中无小于50内的鞋子。所以用来测试是否有幻觉。
1. 输入：帮我推荐跑鞋
2. 继续：要轻量的
3. 继续：预算50以内
4. 继续：不要日系品牌
5. 观察：
   - 系统将轻量预算500排除日系品牌等条件累积到会话状态
   - 检索结果持续收敛
   - 若没有完全匹配，系统应说明当前没有完全满足的商品，而不是返回无关结果

### 3.3 上下文追问
商品品类切换后，会清除上一品类商品的约束，比如预算50以下。
1. 输入：推荐一款保湿眼霜，预算250以内
2. 继续：第二个怎么样
3. 观察：系统基于上轮结果定位第二个商品，并重新展示该商品卡片
4. 继续：再便宜点
5. 观察：系统自动将预算收紧并继续检索更低价商品

### 3.4 复杂约束回归

1. 输入：推荐防晒霜，但我不要含酒精的，也不要日系品牌，
2. 观察：
   - 系统执行多重排除条件
   - 回答中不得编造商品、价格、优惠或库存信息
   - 推荐结果必须来自真实 metadata

### 3.5 主动澄清能力

1. 输入：推荐一款手机
2. 观察：
   - 系统先询问关键偏好（如拍照、续航、性能、预算）
   - 不直接返回商品卡片
3. 继续：拍照优先，预算4000
4. 观察：下一轮查询携带上轮手机主题并继续收敛

### 3.6 商品对比

1. 输入：科颜氏和AHC哪个眼霜更适合干皮
2. 观察：
   - 系统给出对比型回答
   - 端侧展示对比面板和双方候选商品卡片
3. 可选输入：小米和OPPO哪个拍照手机更适合预算4000
4. 观察：系统保留双方差异说明，结论优先推荐符合预算的商品

### 3.7 购物车闭环

1. 输入：推荐一款保湿眼霜，预算250以内
2. 继续：把刚才那款加到购物车
3. 观察：
   - 系统返回加购确认
   - 展示购物车面板
   - 购物车商品行可点击查看详情
4. 继续：把数量改成2
5. 观察：购物车总件数和总价实时更新
6. 可选输入：删除第一个 或 下单吧
7. 观察：系统执行删除或模拟订单确认，不触发真实支付

## 4. 核心验证点

- 最小闭环：客户端、后端、检索、流式、商品卡片、详情页完整打通
- RAG 检索：JSON fallback 与 ChromaStore 均可切换，支持 Ark/Doubao embedding
- 模型链路：`USE_LLM=true` 调用 Doubao/Ark，异常时自动回退模板回答
- 主动澄清：对宽泛需求先补齐预算/偏好，再进入检索和推荐
- 上下文理解：支持第二个怎么样再便宜点等轻量多轮语义
- 商品对比：品牌/商品对比时双侧检索，避免单侧 Top-K 召回偏差
- 购物车能力：自然语言加购、删除、改数量，并通过 cart_update 实时同步端侧
- 防幻觉：价格、品牌、成分等信息直接来源于 metadata
- 架构扩展：工具、输入处理器、后处理器、SSE 事件等链路可插拔

## 5. 备注

- 若演示出现无结果，请先检查 USE_CHROMA、USE_LLM 与 embedding 配置是否一致
- 建议演示从推荐一款…开始，再逐步补充预算用途成分品牌偏好等约束
- 对比场景优先使用哪个更适合类表达，便于 Agent 生成结构化对比结果
