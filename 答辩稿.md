# RAG 多模态电商智能导购 AI Agent — 答辩稿

> 本文档整合项目技术实现、核心流程、评委追问答案，供答辩前复习使用。
> 不含 PPT 页码对应和逐页讲稿。

---

## 一、项目概述

- **技术栈**：Android(Kotlin/Jetpack Compose) + FastAPI(Python) + Chroma 向量库 + Doubao-Seed-2.0-lite 大模型
- **核心能力**：多轮对话、反选排除、上下文追问、图片找货、场景组合推荐、商品对比、对话式购物车
- **数据规模**：100 条商品（美妆/服饰/食品/数码），100 张主图
- **测试覆盖**：35+ 单元测试，12 条离线评估 turns 全部通过

---

## 二、实现评估对照（课题说明会 3.1-4.4）

### 3.1 数据工程与特征治理

| 功能点 | 难度 | 状态 | 说明 | 相关代码 |
|--------|------|------|------|----------|
| 非结构化数据向量化 | ⭐⭐⭐ | ✅ | Doubao-embedding-vision 多模态向量空间；Chroma 真实灌库 + 本地 hashing fallback | `server/rag/embeddings.py` (ArkEmbeddingFunction / HashingEmbeddingFunction) <br> `server/rag/ingest.py` (灌库脚本) |
| 数据一致性保障 | ⭐⭐ | ⚠️ | 当前静态 JSON 数据，架构预留 `VectorStore`/`FactProvider` 接口可替换实时数据源 | `server/rag/vector_store.py` (VectorStore 抽象接口) <br> `server/commerce/facts.py` (FactProvider) |
| Chunking 策略设计 | ⭐ | ✅ | 一商品一主块+一详情块；结构化字段（价格/库存等）单独存 metadata 不进向量 | `server/rag/ingest.py` (build_document_chunks) <br> `server/rag/documents.py` (load_product_documents) |

### 3.2 模型/Agent 编排

| 功能点 | 难度 | 状态 | 说明 | 相关代码 |
|--------|------|------|------|----------|
| RAG 链路可靠性（防幻觉） | ⭐⭐⭐ | ✅ | 三层防护：Prompt 约束 + evidence 绑定 + GroundingGuard 后校验（4条规则） | `server/agent/grounding.py` (guard_grounded_answer) <br> `server/prompts/system_prompt.md` (系统提示词) <br> `server/tools/product_search.py` (to_product_card evidence) |
| 意图识别 | ⭐⭐ | ✅ | `semantic_rules.py` + `intent.py`；`SemanticPlanner` 双层设计（规则 fallback + LLM JSON plan） | `server/agent/semantic_rules.py` (build_rule_plan) <br> `server/agent/intent.py` (detect_intent) <br> `server/agent/semantic_llm.py` (SemanticPlanner.plan) |
| Prompt Engineering | ⭐ | ✅ | 系统提示词严格约束"只能依据候选商品回答" | `server/prompts/system_prompt.md` |

### 3.3 后端服务化

| 功能点 | 难度 | 状态 | 说明 | 相关代码 |
|--------|------|------|------|----------|
| 流式 API（SSE） | ⭐ | ✅ | `POST /chat` 返回 7 种 SSE 事件；WebSocket 路径已豁免超时 | `server/api/chat.py` (event_stream) <br> `server/api/chat.py` (EventResponse 协议) |
| 可扩展网关 | ⭐⭐ | ✅ | 并发限流（100）、请求超时（30s）、熔断器、重试策略、503/504 降级响应 | `server/gateway/middleware.py` (RequestGovernanceMiddleware) <br> `server/gateway/resilience.py` (CircuitBreaker / RetryPolicy) |
| 服务稳定性 | ⭐⭐ | ✅ | 并发限制、超时控制、熔断、降级回退、空检索降级说明 | `server/gateway/middleware.py` <br> `server/agent/recommendation_handler.py` (fallback_answer) <br> `server/gateway/resilience.py` (run_with_timeout) |

### 3.4 客户端工程与体验

| 功能点 | 难度 | 状态 | 说明 | 相关代码 |
|--------|------|------|------|----------|
| 原生流式渲染 | ⭐⭐ | ✅ | Android Compose 逐字流式渲染，仿打字机效果 | `client/app/src/.../ui/ChatScreen.kt` (ChatScreen) <br> `client/app/src/.../ui/MessageComponents.kt` (MessageBubble) |
| 商品卡片组件 | ⭐⭐ | ✅ | 对话流中嵌入可点击商品卡片，支持详情弹窗+本地详情页 | `client/app/src/.../ui/ProductComponents.kt` (ProductCard / DetailDialog) <br> `server/api/products.py` (/products/{id}) |
| 多模态采集 | ⭐⭐⭐ | ⚠️ | 图片上传+视觉签名相似匹配 ✅；语音输入 ❌（架构预留 `ASRProcessor` 接口） | `client/app/src/.../ui/ChatScreen.kt` (图片选择器) <br> `server/inputs/multimodal.py` (MultimodalInputProcessor) <br> `server/inputs/base.py` (ASRProcessor / VLMProcessor 预留) |

### 3.5 基础功能要求（最小闭环）

| 合格标准 | 状态 | 说明 | 相关代码 |
|----------|------|------|----------|
| 数据集 50-100 条 | ✅ | 100 条 `products_ref.json`，覆盖 4 个类目 | `data/products_ref.json` |
| 客户端 Android 原生 App + 对话 + 流式回复 + 商品卡片 | ✅ | 真机端到端 Demo 可运行 | `client/app/src/.../MainActivity.kt` (主入口) <br> `server/api/chat.py` (POST /chat) |
| 后端 FastAPI + 向量库 + RAG + 流式 API | ✅ | Chroma + JSON fallback 双后端 | `server/main.py` (FastAPI 创建) <br> `server/rag/vector_store.py` (ChromaStore / LocalJsonVectorStore) <br> `server/rag/retrieval_pipeline.py` (RAG 检索) |
| 模型理解模糊需求 + 检索 + 推荐理由 + 不编造 | ✅ | SemanticPlanner + 防幻觉三层 | `server/agent/semantic_llm.py` (SemanticPlanner) <br> `server/agent/grounding.py` (GroundingGuard) |

### 4.1 业务闭环深度 — 购物车与下单

| 功能点 | 难度 | 状态 | 说明 | 相关代码 |
|--------|------|------|------|----------|
| 对话式加购 | ⭐ | ✅ | CartHandler 识别意图，修改 SessionState，返回 `cart_update` | `server/agent/commerce_handlers.py` (CartHandler) <br> `server/tools/cart.py` (CartTool) <br> `server/tools/cart_operations.py` (parse_cart_operations) |
| 购物车管理 | ⭐⭐ | ✅ | 加购/删除/改数量/查看/清空，自然语言操作 | `server/tools/cart.py` (execute_cart_operations) <br> `server/tools/cart_operations.py` (CartOperation / infer_operation_action) |
| 下单确认流程 | ⭐⭐⭐ | ⚠️ | 模拟下单（地址确认+订单汇总），未接真实支付系统 | `server/tools/cart.py` (build_checkout_answer_from_cart) |

### 4.2 多模态交互能力

| 功能点 | 难度 | 状态 | 说明 | 相关代码 |
|--------|------|------|------|----------|
| 语音输入 | ⭐ | ❌ | 未实现，架构预留 `ASRProcessor` 接口 | `server/inputs/base.py` (ASRProcessor 预留) |
| TTS 语音播报 | ⭐⭐ | ❌ | 未实现 | 无 |
| 拍照找货 | ⭐⭐⭐ | ✅ | 12×12 RGB 视觉签名 + 相似匹配 + image_summary 拼入查询；优先走多模态 embedding | `server/inputs/multimodal.py` (MultimodalInputProcessor) <br> `server/inputs/image_similarity.py` (image_signature / signature_distance) <br> `server/inputs/visual_embedding.py` (cached_embed_image / cosine_similarity) |
视觉签名用欧式RGB同一维度和位置可以直接加减，多模态是属于神经网络，重点在方向（语义相似）。

### 4.3 对话智能与 RAG 增强

| 功能点 | 难度 | 状态 | 说明 | 相关代码 |
|--------|------|------|------|----------|
| 多轮上下文记忆 | ⭐ | ✅ | SessionState 保存历史+条件+候选；12/12 离线评估 turns 通过 | `server/session/state.py` (SessionState 类) <br> `server/agent/query_rewriter.py` (rewrite_query) <br> `server/agent/context.py` (extract_contextual_filters) |
| 反选与排除 | ⭐⭐ | ✅ | `ExclusionFilter` 程序化过滤，支持一句话多重反选 | `server/rag/post_process.py` (ExclusionFilter) <br> `server/agent/semantic_rules.py` (is_negated_term) |
| 多商品对比决策 | ⭐⭐⭐ | ✅ | `CompareHandler` 分别检索两侧，返回结构化 `comparison_card` | `server/agent/commerce_handlers.py` (CompareHandler) <br> `server/tools/product_compare.py` (build_comparison_answer) |

### 4.4 工程质量与性能优化

| 功能点 | 难度 | 状态 | 说明 | 相关代码 |
|--------|------|------|------|----------|
| 热门查询缓存 | ⭐ | ✅ | Embedding 缓存 SQLite，避免重复计算 | `server/rag/embedding_cache.py` (EmbeddingCache) <br> `server/inputs/visual_embedding.py` (cached_embed_image) |
| 首屏极速响应 | ⭐⭐ | ✅ | 即时 token + 首 Token P95 318-342ms（<1s） | `server/agent/responses.py` (stream_text 即时 token) <br> `scripts/benchmark_first_token.py` (首 Token 压测) |
| 端侧体验打磨 | ⭐⭐⭐ | ⚠️ | 流式渲染+商品卡片 ✅；骨架屏/复杂动画 ❌ | `client/app/src/.../ui/MessageComponents.kt` (流式渲染) |

**统计**：已实现 22 项 ✅ / 部分实现 5 项 ⚠️ / 未实现 2 项 ❌（语音输入、TTS）

---

## 三、核心流程详解

### 3.1 完整消息处理流程（从发出到商品卡片）

```
用户输入（Android 端）
    ↓ OkHttp SSE 长连接
FastAPI /chat 接收 → Pydantic 校验 → 解析图片 payload
    ↓
Orchestrator.stream_chat() 启动 13 步处理

Step 1: 向前端推送 "event: status" {state: "thinking"}
Step 2: 获取/创建 Session（SQLite/Redis 恢复历史状态）sql是本地，redis是远程服务器sql不能并发（python的线程锁），redis可以
Step 3: 记录用户消息到 session.history
Step 4: 输入处理
    → 文本：分词/关键词/数量解析（TextProcessor）
    → 图片：12×12 视觉签名 或 多模态 embedding 向量比对
    → 生成 image_summary，拼入查询文本
    → 如有图片：yield "event: image_analysis"
Step 5: 语义规划 SemanticPlanner.plan()
    → Layer 1: 规则快速匹配（关键词、正则、词表）→ 低延迟、确定性高
    → Layer 2: 0.8 秒短预算请求 LLM 生成 JSON plan
    → 超时/失败/非法 JSON → 自动回退规则层
    → 合并规则 + LLM 结果 → Pydantic 校验
Step 6: 上下文追问过滤条件提取
    → "再便宜点" → 追加低于上一轮最低价的预算上限
    → "要轻量的" → 追加关键词"轻量"
Step 7: 品类切换判断 scope_transition
    → 检测 product_type 变化（跑鞋→手机）
    → 若变化：reset_product_scope() 清理旧 filters/exclusions/candidates
    → 防止旧品类污染新需求
Step 8: 合并过滤条件到 session（merge_filters）
    → 多轮条件累积：预算 200 + 品牌科颜氏 = 同时满足
Step 9: 构建 AgentTurnContext（打包所有上下文）
Step 10: AgentWorkflow 按优先级遍历 Handler
    → 1. ClarificationHandler（宽泛先追问）
    → 2. CartHandler（购物车操作）
    → 3. CompareHandler（X vs Y 对比）
    → 4. ScenarioBundleHandler（场景组合）
    → 5. ContextFollowUpHandler（"第二个""再便宜点"）
    → 6. RecommendationHandler（兜底推荐）
    → 第一个 matches() 返回 True 的执行，其余跳过
Step 11: 查询反馈记录（plan/handler/命中，用于离线评估）
Step 12: Trace 落盘（/debug/traces 可查看完整链路）
Step 13: finally 刷新并保存 Session（refresh + save，保证状态不丢失）
    ↓
SSE 流式返回给 Android
    → event: token（逐字推送）
    → event: product_card（商品卡片）
    → event: comparison_card（对比面板）
    → event: cart_update（购物车状态）
    → event: guardrail（防幻觉拦截）
    → event: done（本轮结束）
    ↓
Android 前端按 event 类型分发渲染
    → token: 逐字追加到消息气泡（打字机效果）
    → product_card: 渲染商品卡片（主图+名称+价格+品牌，可点击）
    → comparison_card: 渲染对比面板（推荐结论+双方优劣势）
    → cart_update: 展开购物车面板（总件数+总价+商品列表）
```

**相关代码**：`server/agent/orchestrator.py` (stream_chat 13 步核心) <br> `server/api/chat.py` (POST /chat SSE 接口) <br> `client/app/src/.../network/ChatSseClient.kt` (Android SSE 客户端) <br> `client/app/src/.../ui/ChatScreen.kt` (前端渲染)

---

### 3.2 图片检索完整流程

**方案 A（优先）：多模态 Embedding**

```
用户上传图片
    ↓
Android 发送 image_base64 或 image_id
    ↓
MultimodalInputProcessor.process()
    → 检查 visual_embedding_index 是否可用
    → 可用：prepare_image_for_embedding() 预处理
        → 转 RGB → 缩放到 1024×1024 以内 → JPEG 质量 85%
    → cached_embed_image() 获取查询向量
        → 先查 SQLite 缓存（content_hash 命中？）
        → 未命中：HTTP POST /embeddings/multimodal
            → model: "doubao-embedding-vision"
            → input: [{"type": "image_url", "image_url": {"url": "data:image/jpeg;base64,..."}}]
            → 返回 768 维向量
        → 写入缓存
    → 和商品库中预先生成的图片向量做 cosine_similarity
    → 返回 top 3 最相似商品
    → 生成 image_summary：
        "我先按图片做了视觉匹配，当前商品库里最接近的是「XX运动鞋」，
         品牌是耐克，类目是运动鞋，相似度约 0.85"
    → 拼入用户查询，走正常 RAG 检索
```

**方案 B（Fallback）：视觉签名**

```
用户上传图片
    ↓
image_similarity.py 的 image_signature()
    → 打开图片，转 RGB
    → 缩放到 12×12 像素
    → 提取每个像素的 RGB 值，归一化到 0-1
    → 得到 432 维向量（12×12×3=432）
    → 和商品库所有主图的 432 维签名比对
    → 计算欧氏距离（越小越相似）
    → 返回 top 3
```

**商品库图片向量是怎么来的**（`build_product_visual_index`）：

```
遍历 100 个商品
    → 读取商品主图（data/product_images/xxx.jpg）
    → 预处理（转 RGB、缩放、JPEG）
    → 计算 content_hash（SHA256）
    → 检查是否已索引（避免重复）
    → cached_embed_image() 获取向量（或视觉签名）
    → 保存到 visual_index.json：
        {
            "product": {id, name, brand, category},
            "image_name": "p_beauty_001.jpg",
            "content_hash": "abc123...",
            "vector": [0.023, -0.156, ...]
        }
```

**相关代码**：`server/inputs/multimodal.py` (MultimodalInputProcessor.process) <br> `server/inputs/image_similarity.py` (image_signature / signature_distance) <br> `server/inputs/visual_embedding.py` (cached_embed_image / cosine_similarity) <br> `server/inputs/visual_embedding.py` (build_product_visual_index 灌库)

---

### 3.3 商品卡片生成与跳转

**后端：`to_product_card()` 格式化**

```python
商品卡片 = {
    "id": "p_beauty_021",
    "name": "科颜氏高保湿面霜",
    "brand": "科颜氏",
    "price": 320,
    "category": "美妆",
    "image_url": "http://.../p_beauty_021_live.jpg",
    "detail_url": "http://.../products/p_beauty_021",
    "reason": "与查询匹配度高",
    "score": 0.85,
    "evidence": {
        "id": "p_beauty_021",
        "brand": "科颜氏",
        "price": 320,
        "category": "美妆",
        "score": 0.85
    }
}
```

**前端：Android 渲染流程**

```
收到 "event: product_card"
    ↓
ProductCard Composable 渲染
    ┌─────────────────────┐
    │  [商品主图 - Coil 加载]│
    │  科颜氏高保湿面霜    │
    │  ¥320               │
    │  品牌：科颜氏        │
    │  [查看详情] 按钮     │
    └─────────────────────┘
    ↓ 点击
弹出 ProductDetailDialog
    ┌─────────────────────┐
    │  [大图]              │
    │  名称/价格/品牌/类目 │
    │  [打开链接] 按钮     │
    └─────────────────────┘
    ↓ 点击 "打开链接"
浏览器打开 detail_url = http://.../products/p_beauty_021
    → FastAPI 返回本地 HTML 详情页
```

**相关代码**：`server/tools/product_search.py` (to_product_card) <br> `server/api/products.py` (/products/{id}) <br> `client/app/src/.../ui/ProductComponents.kt` (ProductCard / ProductDetailDialog)

---

### 3.4 商品对比完整流程

```
用户输入："科颜氏眼霜和AHC眼霜哪个好？"
    ↓
CompareHandler.matches() 检测到 plan.intent == "compare"
    ↓
CompareHandler.handle()
    → registry.execute("compare_products", query=..., filters=...)
    ↓
ProductCompareTool 解析两侧
    → 从消息中解析两个对比对象：品牌 A = "科颜氏"，品牌 B = "AHC"
    → 构建两个独立查询：
        查询 A: "科颜氏眼霜" + filters={brand: ["科颜氏"]}
        查询 B: "AHC眼霜" + filters={brand: ["AHC"]}
    → 分别调用 ProductSearchTool 检索两侧
    → 各自返回 Top-K 商品
    → 强制保证双方都有候选（避免 Top-K 只召回一个品牌）
    ↓
生成对比结构
    ┌─────────┬──────────┬──────────┐
    │  维度    │  科颜氏   │  AHC     │
    ├─────────┼──────────┼──────────┤
    │ 价格     │ ¥320     │ ¥189     │
    │ 容量     │ 14ml     │ 30ml     │
    │ 成分     │ 维生素C  │ 玻尿酸   │
    │ 适用     │ 提亮/全肤质│ 熬夜/干皮│
    │ 检索分   │ 0.91     │ 0.88     │
    └─────────┴──────────┴──────────┘
    → 优先推荐预算内商品
    → 超预算商品在 tradeoffs 中标注
    ↓
生成自然语言回答 + 返回 SSE 事件
    → event: token → "两款各有侧重..."
    → event: product_card → 科颜氏商品
    → event: product_card → AHC商品
    → event: comparison_card → 结构化对比数据
    → event: done
```

**相关代码**：`server/agent/commerce_handlers.py` (CompareHandler) <br> `server/tools/product_compare.py` (build_comparison_answer) <br> `client/app/src/.../ui/ComparisonPanel.kt` (Android 对比面板)

---

### 3.5 对话式购物车完整流程（含"第二个"引用）

```
用户输入："把第二个加到购物车"
    ↓
AgentWorkflow 遍历 Handler
    → CartHandler.matches()：
        plan.intent == "cart" → 匹配成功
    ↓
CartHandler.handle() → registry.execute("manage_cart")
    ↓
CartTool.run() → parse_cart_operations(message, plan)
    → 解析结果：
        CartOperation(
            action="add",
            segment="把第二个加到购物车",
            target_index=1,  # 索引从0开始，ordinal=2→index=1
            quantity=1
        )
    ↓
resolve_candidate_product()
    → 检查 operation.target_index = 1
    → 从 session.candidate_product_cards 取索引 1 的商品
    → 假设索引 1 = "AHC 眼霜 ¥189"
    ↓
apply_add(working_cart, resolved_product, quantity=1)
    → 商品加入 working_cart：
        {
            "product_id": "p_beauty_022",
            "name": "AHC 眼霜",
            "brand": "AHC",
            "price": 189,
            "quantity": 1
        }
    → session.cart = working_cart
    ↓
生成回答
    → "已将 AHC 眼霜 加入购物车，数量 1。购物车目前有 1 件商品，总价 ¥189。"
    ↓
返回 SSE 事件
    → event: token → 回答文字
    → event: cart_update → 购物车结构化数据
    → event: done
```

**模糊购买确认（"那支给我来两件"）**：

```
用户输入："那支给我来两件"
    ↓
is_add_quantity_expression() = True
但用户没有明确说"加入购物车"
    ↓
进入 prepare_purchase_confirmation()（购买确认模式）
    → session.pending_cart_action = {
        "action": "add",
        "quantity": 2,
        "candidates": [候选商品列表],
        "source": "purchase_confirmation"
    }
    → 返回："我理解你想买 2 件 AHC 的这款：AHC 眼霜。确认把它加入购物车吗？"
    ↓
用户回复："确认"
    → handle_pending_cart_action()
    → 从 candidates 中选商品，加入购物车
    → 清空 pending_cart_action
    → 返回："已将 AHC 眼霜 加入购物车，数量 2。"
```

**相关代码**：`server/agent/commerce_handlers.py` (CartHandler) <br> `server/tools/cart.py` (CartTool / execute_cart_operations / handle_pending_cart_action) <br> `server/tools/cart_operations.py` (CartOperation / parse_cart_operations / infer_operation_action) <br> `client/app/src/.../ui/CartPanel.kt` (Android 购物车面板)

---

### 3.6 会话状态管理（历史增加与清理）

**SessionState 核心字段**：

```python
session = {
    "history": [],                    # 对话历史（用户+助手消息）
    "filters": [],                    # 正向过滤条件（预算、品牌、关键词）
    "exclusions": [],                 # 排除条件（"不要日系"）
    "candidate_products": [],         # 上一轮候选商品 ID 列表
    "candidate_product_cards": [],    # 上一轮候选商品完整卡片
    "pending_subject": "",            # 待澄清主题（如"眼霜"）
    "pending_cart_action": {},        # 待确认购物车操作
    "cart": [],                       # 购物车商品列表
}
```

**历史增加**：

```python
# 每轮对话自动追加
session.add_user_message("推荐一款眼霜")        # history.append({role:"user", content:"..."})
session.add_assistant_message("我为您推荐...")  # history.append({role:"assistant", content:"..."})
```

**品类切换时的清理**：

```
用户先聊"眼霜"（filters=[product_type="眼霜", max_price=200]）
    ↓
用户说"那给我推荐个手机吧"
    → SemanticPlanner 解析出 product_type="手机"
    → scope_transition_policy 检测到品类变化
    → reset_product_scope() 执行：
        filters = []              # 清空旧预算、旧关键词
        exclusions = []           # 清空旧排除条件
        candidate_products = []   # 清空上一轮候选
        candidate_product_cards = []  # 清空候选卡片
        pending_subject = ""      # 清空待澄清主题
        pending_cart_action = {}  # 清空待确认购物车操作
    → 但保留：
        history = [4条对话历史]     # 保留！LLM 可从历史恢复上下文
        cart = [购物车商品]         # 保留！购物车跨品类不丢失
```

**答辩金句**：
> "品类切换时清理的是过滤条件和候选商品，但保留对话历史。这是因为'眼霜的预算 200 块'不应该带到'手机'搜索里，但用户说'再便宜点'时，LLM 仍然可以从历史中恢复上下文。清理是设计上的有意权衡：宁可让用户重新说条件，也不让旧条件错误污染新检索。"

**相关代码**：`server/session/state.py` (SessionState 类 / reset_product_scope) <br> `server/agent/scope_transition.py` (scope_transition_policy)

---

### 3.7 SemanticPlanner 双层设计

```
用户输入进来
    ↓
┌─────────────────────────────────────────┐
│  Layer 1: 规则层（本地，零延迟）         │
│  build_rule_plan()                      │
│  → 关键词匹配、正则、词表查找             │
│  → 快速确定：intent、filters、预算、品牌   │
│  → 适用于：明确意图、明确预算、明确购物车操作 │
└─────────────────────────────────────────┘
    ↓
检查：should_try_llm_planning()
    → 判断规则结果是否足够可信？
    → 标准：
        ① 消息中有模糊表达（"适合熬夜党的"）
        ② 有否定/反选（"不要含酒精的"）
        ③ 有数量词（"来两件"）
        ④ 有引用（"第二个""那款"）
        ⑤ 有复杂条件组合
        ⑥ 规则 confidence < 0.5
    → 不需要 → 直接返回规则结果
    → 需要 → 进入 Layer 2
    ↓
┌─────────────────────────────────────────┐
│  Layer 2: LLM 层（外接 API，有延迟）     │
│  _try_llm_plan()                        │
│  → 用 Doubao-Seed 生成 JSON 格式的规划    │
│  → 预算：0.8 秒（超时自动回退）            │
│  → 返回非法 JSON → 返回 None              │
│  → Pydantic 校验失败 → 返回 None          │
└─────────────────────────────────────────┘
    ↓
    LLM 返回有效？
    ├─ 是 → merge_semantic_plans() → 合并规则 + LLM 结果
    │         → 规则 confident 的字段保留
    │         → LLM 新发现的字段补充
    │         → 去重合并 → Pydantic 校验 → 返回 plan
    └─ 否 → 直接返回规则结果
```

**具体场景对照**：

| 用户输入 | 规则层能否处理？ | 实际走哪层 |
|----------|----------------|-----------|
| "推荐一款手机" | ✅ 能 | 规则层 |
| "预算 500 以内，轻量的" | ✅ 能 | 规则层 |
| "不要含酒精的" | ⚠️ 部分能 | 规则 + LLM 增强 |
| "那支给我来两件" | ❌ 不能 | LLM 层 |
| "第二个怎么样" | ⚠️ 部分能 | 规则层（confidence 低时走 LLM） |
| "有没有比第二款更适合敏感肌但别太贵的" | ❌ 不能 | LLM 层 |

**相关代码**：`server/agent/semantic_llm.py` (SemanticPlanner.plan / should_try_llm_planning) <br> `server/agent/semantic_rules.py` (build_rule_plan) <br> `server/agent/planning_policy.py` (PlannerPolicy)

---

### 3.8 RAG 检索 Pipeline 5 阶段

```
Stage 1: Store 预过滤召回
    → candidate_multiplier=10, min_pool=50
    → 先召回 top_k × 10 = 50 条
    → 如果带过滤没结果，自动放宽过滤再查一次（防空结果）
    ↓
Stage 2-7: 7 个 PostProcessor 依次过滤
    → RangeFilter（价格区间）
    → ProductTypeFilter（商品类型）
    → CategoryFilter（大类）
    → BrandFilter（品牌）
    → StockFilter（库存）
    → KeywordFilter（关键词严格匹配）
    → ExclusionFilter（反选排除）
    → 注意：程序化过滤，不靠 AI 判断
    ↓
Stage 8: Dedupe 去重（按商品 ID 去重）
    ↓
Stage 9: Rerank 重排序（规则化加权排序）
    → 第一步：混合打分 = 向量相似度分 + 关键词匹配分
        → 向量分：Chroma 返回 distance，转成 1.0/(1.0+distance)
        → 关键词分：token 重叠 + 短语匹配 + 标题匹配
    → 第二步：规则加权加分
        ┌─────────────┬──────┬────────────┐
        │ 匹配项       │ 加分 │ 优先级说明  │
        ├─────────────┼──────┼────────────┤
        │ product_type │ +20 │ 最高：精确商品类型 │
        │ category     │ +12 │ 次高：大类匹配     │
        │ keyword      │ +6  │ 中等：关键词命中   │
        │ brand        │ +5  │ 中等：品牌命中     │
        │ 预算内       │ +2  │ 基础：价格符合预算 │
        │ 查询直接提及  │ +8  │ 额外：用户说商品名 │
        └─────────────┴──────┴────────────┘
    → 结构化匹配优先于关键词匹配
    → 不是神经网络 reranker（如 BGE-Reranker），而是规则加权，可控性更强
    → 万级数据扩展时，可在 Pipeline 中替换为外部 reranker
    ↓
```

**相关代码**：`server/rag/retrieval_pipeline.py` (ProductRetrievalPipeline.run / rerank_hits / relevance_bonus) <br> `server/rag/post_process.py` (7 个 PostProcessor) <br> `server/rag/stores.py` (ChromaStore.query / LocalJsonVectorStore.query) <br> `server/rag/scoring.py` (score_chroma_hit / score_document_features)

---

### 3.9 GroundingGuard 防幻觉三层

```
LLM 生成回答
    ↓
Layer 1: Prompt 约束
    → 系统提示词："你只能依据候选商品回答，不编造价格、优惠、库存、销量"
    ↓
Layer 2: 证据绑定
    → 每个商品卡片携带结构化 evidence：
        {id, brand, price, category, score}
    → LLM 生成时必须引用这些真实数据
    ↓
Layer 3: 后校验（4 条规则）
    → ① 是否提到未提供的优惠/库存/销量/好评率
    → ② 是否提到候选商品和预算外的价格
    → ③ 是否没有引用任何候选商品名称/品牌
    → ④ 是否有绝对化/医疗化承诺（"根治""100%""保证"）
    → 触发任一：yield "event: guardrail" + 改用模板回答
    → 未触发：LLM 回答正常发送
```

**相关代码**：`server/agent/grounding.py` (guard_grounded_answer / GroundingGuard) <br> `server/prompts/system_prompt.md` (Prompt 约束) <br> `server/agent/recommendation_handler.py` (LLM 生成 + GroundingGuard 调用) <br> `server/tools/product_search.py` (to_product_card evidence)

---

### 3.10 6 大抽象接口与扩展点

| 接口 | 当前实现 | 预留扩展 | 相关代码 |
|------|----------|----------|----------|
| **VectorStore** | ChromaStore + LocalJsonStore | QdrantStore / MilvusStore | `server/rag/vector_store.py` (VectorStore 协议) |
| **ToolRegistry** | search + compare + cart | 新增工具只注册，不动编排 | `server/tools/registry.py` (ToolRegistry) |
| **InputProcessor** | Text + Multimodal | ASRProcessor / VLMProcessor | `server/inputs/base.py` (InputProcessor) |
| **PostProcessor** | 7 个过滤器责任链 | 新增过滤器即插即用 | `server/rag/post_process.py` (PostProcessor) |
| **SSE 事件协议** | 7 种事件类型 | 新类型向后兼容，老客户端忽略未知 | `server/api/chat.py` (SSE 事件协议) |
| **SessionStore** | memory + sqlite + redis | 切换后端只改配置 | `server/session/state.py` (SessionStore / PersistentSessionStore) |

**额外预留**：
- `AgentWorkflow` → 可替换为 LangGraph | `server/agent/workflow.py` (AgentWorkflow)
- `SemanticPlanner` → 可扩展更复杂 planner | `server/agent/semantic_llm.py` (SemanticPlanner)
- `ProductRetrievalPipeline` → 可接入 BM25 / 外部 reranker | `server/rag/retrieval_pipeline.py` (ProductRetrievalPipeline)
- `ScenarioCatalog` → 新增场景只需扩展 JSON | `server/agent/scenarios.py` (ScenarioCatalog)

---

## 四、Embedding 详解

### 4.1 两套 Embedding 方案

| 方案 | 使用条件 | 代码位置 | 特点 | 相关代码 |
|------|---------|----------|------|----------|
| **Doubao-embedding-vision** | 有 API Key 时 | `embeddings.py` `ArkEmbeddingFunction` / `ArkMultimodalEmbeddingFunction` | 真实语义向量，768 维，支持文本+图片多模态 | `server/rag/embeddings.py` (ArkEmbeddingFunction / ArkMultimodalEmbeddingFunction) |
| **Hashing Embedding** | 无 API Key 时（fallback） | `embeddings.py` `HashingEmbeddingFunction` | 本地哈希，384 维，无需网络，精度低但可运行 | `server/rag/embeddings.py` (HashingEmbeddingFunction) <br> `server/rag/scoring.py` (hashing_embedding / tokenize) |

### 4.2 文本 Embedding 完整流程

**灌库阶段**：

```
读取 products_ref.json（100 条商品）
    ↓
每条生成 Document：
    page_content = "商品名称 + 品牌 + 类目 + 描述"
    metadata = {id, name, brand, category, price, stock, ...}
    ↓
判断：USE_CHROMA=true 且 ARK_API_KEY 有值？
    ├─ 是 → 创建 ArkEmbeddingFunction
    │         → model="doubao-embedding-vision"
    │         → HTTP POST /embeddings
    │         → 返回 768 维向量
    │         → collection_name 带 "ark_embedding_" 前缀
    └─ 否 → 创建 HashingEmbeddingFunction
              → 分词 → 哈希 → 映射到 384 维
              → collection_name 无前缀
    ↓
Chroma collection.add(id, embedding, document, metadata)
```

**相关代码**：`server/rag/ingest.py` (main 灌库入口) <br> `server/rag/embeddings.py` (build_chroma_embedding_function) <br> `server/rag/stores.py` (ChromaStore.add)

**查询阶段**：

```
用户输入："适合油皮的洗面奶"
    ↓
ChromaStore.query()
    → 调用同一个 embedding 模型把查询转成向量
    → collection.query(
        query_embeddings=[query_vector],
        n_results=50,          # 粗召回 50 条
        where={price: {$lte: 200}, stock: {$gt: 0}}  # metadata 预过滤
      )
    → 返回最接近的 50 条商品
```

**相关代码**：`server/rag/stores.py` (ChromaStore.query / LocalJsonVectorStore.query) <br> `server/rag/retrieval_pipeline.py` (ProductRetrievalPipeline.run Stage 1)

### 4.3 图片 Embedding 完整流程

**多模态 Embedding（有 API Key）**：

```
用户上传图片
    → 预处理：转 RGB → 缩放到 1024×1024 → JPEG 质量 85%
    → cached_embed_image()
        → 计算 content_hash → 查 SQLite 缓存
        → 未命中：HTTP POST /embeddings/multimodal
            → model: "doubao-embedding-vision"
            → input: [{"type": "image_url", "image_url": {"url": "data:image/jpeg;base64,…"}}]
            → 返回图片向量（768 维）
        → 写入缓存
    → 和商品库图片向量做 cosine_similarity
    → 返回 top 3 最相似商品
```

**相关代码**：`server/inputs/multimodal.py` (MultimodalInputProcessor._match_image) <br> `server/inputs/visual_embedding.py` (match_image_bytes / match_base64_image)

**视觉签名（无 API Key）**：

```
用户上传图片
    → 打开图片，转 RGB
    → 缩放到 12×12 像素
    → 提取每个像素的 RGB 值，归一化到 0-1
    → 得到 432 维向量（12×12×3=432）
    → 和商品库主图签名计算欧氏距离
    → 距离越小越相似，返回 top 3
```

**相关代码**：`server/inputs/image_similarity.py` (image_signature / signature_distance) <br> `server/inputs/image_similarity.py` (build_image_index)

**商品库图片向量灌库**：

```
遍历 100 个商品
    → 读取主图文件
    → 预处理 → 计算 content_hash
    → 检查是否已索引（跳过重复）
    → 获取向量（多模态 embedding 或视觉签名）
    → 保存到 visual_index.json：
        {product, image_name, content_hash, vector}
```

**相关代码**：`server/inputs/visual_embedding.py` (build_product_visual_index) <br> `server/inputs/visual_embedding.py` (cached_embed_image) <br> `server/inputs/visual_embedding.py` (prepare_image_for_embedding)

### 4.4 Embedding 缓存机制

```python
class EmbeddingCache:
    # SQLite 持久化缓存
    # 表结构：cache_key, provider, model, modality, content_hash, dimensions, vector_json
    
    def get(self, key: EmbeddingCacheKey) -> list[float] | None:
        # 查 SQLite：SELECT vector_json FROM embeddings WHERE cache_key = ?
        # 命中 → 返回向量
        # 未命中 → 返回 None
    
    def set(self, key, vector, metadata):
        # UPSERT：INSERT ... ON CONFLICT(cache_key) DO UPDATE SET ...
```

**缓存键计算**：

```python
EmbeddingCacheKey(
    provider="ark_multimodal",
    model="doubao-embedding-vision",
    modality="image",  # 或 "text"
    content_hash=SHA256(图片/文本内容)
).storage_key = SHA256(f"{provider}\0{model}\0{modality}\0{content_hash}")
```

**特点**：
- 同一个内容第二次查询直接读 SQLite，不调用 API
- 缓存键包含 provider + model + modality，换模型自动隔离
- 数据库有索引，查询毫秒级

**相关代码**：`server/rag/embedding_cache.py` (EmbeddingCache) <br> `server/inputs/visual_embedding.py` (cached_embed_image)

### 4.5 相似度计算

**余弦相似度（多模态 embedding）**：

```python
cosine_similarity = dot_product / (norm_a * norm_b)
# 1.0 = 完全相同方向
# 0.0 = 完全正交
```

**相关代码**：`server/inputs/visual_embedding.py` (cosine_similarity)

**欧氏距离（视觉签名）**：

```python
signature_distance = sqrt(sum((a-b)^2) / len)
# 0.0 = 完全相同
# 1.0 = 完全不同（被截断到 1.0）
```

**相关代码**：`server/inputs/image_similarity.py` (signature_distance)

### 4.6 答辩金句

> "Embedding 的核心价值是实现语义检索。传统关键词搜索只能匹配字面相同的词，比如用户搜'油皮'，如果商品标题里没有这个词就找不到。但 Embedding 把文本转成高维向量，能捕获语义关联——'油皮'和'控油'在向量空间里是接近的。
>
> 我们用了豆包的 doubao-embedding-vision，它是多模态模型，同时支持文本和图片的向量编码。这样用户输入文字或上传图片，都能进入同一个向量空间做相似度比较。
>
> 我们还做了缓存机制（SQLite 持久化缓存）和 fallback 设计（无 API Key 时用本地 hashing），避免重复计算和保证开箱可运行。"

---

### 4.7 向量相似度与重排序机制

#### 向量相似度比较

分两个场景：

**场景 A：Chroma 向量数据库内部（有 API Key 时）**

Chroma 内部使用 **HNSW（Hierarchical Navigable Small World）近似最近邻算法**。

返回的是 `distance`（距离值），不是直接相似度。代码里转成相似度分数：

```python
vector_score = 1.0 / (1.0 + distance)
```

- **distance 越小**（向量越接近）→ **vector_score 越高**
- 这个公式是**距离的倒数映射**，不是严格的余弦相似度，但效果等价

**场景 B：LocalJsonVectorStore（无 API Key 的 fallback）**

这里**不是真正的向量相似度比较**。用的是 **关键词匹配打分**：

```python
overlap = len(query_tokens & features.tokens)        # 共有词数量
phrase_bonus = sum(2 for token in query_tokens if token in features.haystack)   # 短语匹配
title_bonus = sum(6 for token in query_tokens if token in features.title_text)    # 标题匹配
return float(overlap + phrase_bonus + title_bonus)
```

简单说：数一下用户查询的词和商品文档有多少词重合，重合越多分越高。

#### 重排序（Rerank）

**规则化加权重排序（Rule-based Weighted Reranking）**，不是神经网络 reranker。

**第一步：混合打分（向量 + 关键词）**

```python
score_chroma_hit = lexical_score + vector_score
# lexical_score = 关键词匹配分（token 重叠 + 短语 + 标题）
# vector_score = 1.0 / (1.0 + distance)  # 向量相似度分
```

**第二步：规则加权加分**

在基础分数上，按结构化匹配程度**逐项加固定分值**：

| 匹配项 | 加分 | 优先级说明 |
|--------|------|------------|
| **商品类型（product_type）** | **+20** | 最高：如"洗面奶"精确匹配 |
| 大类（category） | +12 | 次高：如"美妆"类目匹配 |
| 关键词（keyword） | +6 | 中等：如"油皮""保湿"命中 |
| 品牌（brand） | +5 | 中等：如"科颜氏"命中 |
| 预算内 | +2 | 基础：价格符合用户预算 |
| 查询直接出现商品名/品牌名 | +8 | 额外：用户直接提到某商品 |

**重排序的本质**：

```
初始召回：Chroma 按向量距离排序 → 50 条
    ↓
Rerank：每条重新算分 = 向量相似度分 + 关键词匹配分 + 规则加权
    ↓
按总分从高到低排序 → 取 Top-5
```

#### 答辩金句

> "向量相似度比较分两个场景：有 API Key 时，Chroma 内部用 HNSW 近似最近邻，返回 distance 值，代码里转成 1.0/(1.0+distance) 作为相似度分；无 API Key 的 fallback 模式，用关键词重叠计数（token overlap + phrase bonus + title bonus）来打分。
>
> 重排序我们没有用外部 reranker 模型（如 BGE-Reranker），而是采用混合打分 + 规则加权重排序。原因是：100 条数据规模下，规则加权足够有效，且延迟更低、可控性更强。后续如果数据量扩展到万级，可以在当前架构中接入外部 reranker，只需要替换 Pipeline 的 rerank 阶段，其他层不动。"

**相关代码**：`server/rag/retrieval_pipeline.py` (rerank_hits / relevance_bonus) <br> `server/rag/scoring.py` (score_chroma_hit / score_document_features) <br> `server/rag/stores.py` (ChromaStore.query / LocalJsonVectorStore.query) <br> `server/rag/vector_store.py` (VectorStore 抽象)

---

## 五、评委高频追问与标准答案

### Q1：会话状态存了哪些字段？品类切换时怎么处理？（`server/session/state.py`）

**相关代码**：`server/session/state.py` (SessionState 类) <br> `server/session/state.py` (reset_product_scope / starts_new_product_scope) <br> `server/agent/scope_transition.py` (scope_transition_policy)

> "会话状态存了 6 个核心字段：对话历史、正向过滤条件、排除条件、上一轮候选商品、购物车、待澄清主题。
>
> 当检测到用户切换了品类，比如从'眼霜'变成'手机'，我们自动清理旧的过滤条件和排除条件，防止'200 块'这种眼霜预算被带到手机搜索里。但对话历史会保留，所以用户说'再便宜点'时，系统仍然知道是在上一轮候选里找更便宜的。"

---

### Q2：怎么换向量数据库？没有 API Key 能跑吗？（`server/rag/vector_store.py`）

**相关代码**：`server/rag/vector_store.py` (VectorStore 协议) <br> `server/rag/stores.py` (ChromaStore / LocalJsonVectorStore) <br> `server/rag/embeddings.py` (ArkEmbeddingFunction / HashingEmbeddingFunction)

> "我们定义了一个抽象接口 VectorStore，只暴露三个方法：add、query、delete。当前有 Chroma 和本地 JSON 两个实现。换数据库时只需要改配置或新增一个类实现这三个方法，主流程代码一行不用改。
>
> 没有 API Key 时，本地 JSON 实现用倒排索引和关键词匹配，照样能检索，保证项目开箱可运行。"

---

### Q3：ClarificationHandler 为什么排第一？"第二个怎么样"怎么定位？（`server/agent/conversation_handlers.py`）

**相关代码**：`server/agent/conversation_handlers.py` (ClarificationHandler / ContextFollowUpHandler) <br> `server/agent/semantic_rules.py` (build_rule_plan / reference_type) <br> `server/session/state.py` (candidate_product_cards)

> "ClarificationHandler 排第一，是为了防止用户说'推荐一款手机'这种宽泛需求时，系统直接硬推商品，而是先追问预算和偏好。
>
> 当用户说'第二个怎么样'，语义规划层会识别 reference_type=ordinal，然后 ContextFollowUpHandler 直接取上一轮候选列表的第 2 个商品，不用重新检索。"

---

### Q4：加新工具比如"收藏"需要改很多地方吗？（`server/tools/registry.py`）

**相关代码**：`server/tools/registry.py` (ToolRegistry) <br> `server/agent/workflow.py` (AgentWorkflow / Handler 遍历)

> "不需要。我们有一个 ToolRegistry 注册表，本质上是一个字典。新增功能只需要写一行 registry.register('收藏', 收藏工具对象)，然后对应的 Handler 里调用这个工具名就行。Workflow 的调度逻辑、API 接口、甚至前端协议都不需要改。"

---

### Q5：SSE 怎么实现？前端怎么区分文字和商品卡片？（`server/api/chat.py`）

**相关代码**：`server/api/chat.py` (POST /chat / event_stream) <br> `client/app/src/.../network/ChatSseClient.kt` (Android SSE 客户端) <br> `client/app/src/.../viewmodel/ChatViewModel.kt` (事件分发)

> "后端用 FastAPI 的 StreamingResponse 建立一条长连接，数据生成一段就推一段，前端收到一段渲染一段，所以能看到逐字打字效果。
>
> 我们定义了统一的事件协议，每个消息带一个 event 字段，比如 event: token 是文字、event: product_card 是商品卡片、event: cart_update 是购物车状态。前端按事件类型分发渲染，新增事件类型时老客户端会自动忽略，保证向后兼容。"

---

### Q6：LLM 调用超时或失败怎么办？（`server/llm/ark_client.py`）

**相关代码**：`server/llm/ark_client.py` (ArkChatClient) <br> `server/agent/semantic_llm.py` (SemanticPlanner._try_llm_plan) <br> `server/agent/recommendation_handler.py` (collect_llm_answer_with_budget) <br> `server/gateway/resilience.py` (run_with_timeout / CircuitBreaker)

> "每一层都有超时预算：SemanticPlanner 是 0.8 秒，LLM 生成也有独立预算，超时自动抛异常被外层捕获。调用失败或超时时，不会返回错误给用户，而是自动降级到本地模板回答。
>
> 即使用户没有配置 API Key，或者网络不通，系统也能正常返回商品卡片，只是没有 AI 生成的个性化推荐理由。"

---

### Q7：API Key 怎么管理？生产和开发怎么区分？（`server/config.py`）

**相关代码**：`server/config.py` (Settings 类) <br> `.env.example` (配置模板) <br> `.gitignore` (排除 .env)

> "所有敏感配置都存在 .env 文件里，并且 .env 已经加入 .gitignore，确保永远不会提交到代码仓库。代码里用 pydantic-settings 读取环境变量，区分 APP_ENV=development 和 production，生产环境自动关闭调试接口和错误堆栈。部署时只需要在服务器上放一份 .env，不用改任何代码就能切换环境。"

---

### Q8：你们的数据量只有 100 条，怎么保证召回效果？

**相关代码**：`server/rag/vector_store.py` (VectorStore 抽象) <br> `scripts/evaluate_esci_retrieval.py` (ESCI small 评测) <br> `scripts/benchmark_retrieval.py` (检索压测)

> "当前 100 条是为了快速验证链路。我们在架构上做了两层保障：一是本地 JSON fallback 支持 50K 数据毫秒级检索；二是接了 ESCI small 外部数据集 5549 条商品做评测，Recall@K=0.47，证明检索策略有效。生产环境可以无缝切换到万级 Chroma。"

---

### Q9：大模型幻觉怎么解决？如果 LLM 编造价格呢？

**相关代码**：`server/agent/grounding.py` (GroundingGuard / guard_grounded_answer) <br> `server/prompts/system_prompt.md` (Prompt 约束) <br> `server/tools/product_search.py` (to_product_card evidence) <br> `server/agent/recommendation_handler.py` (LLM 生成 + guard 校验)

> "我们三层防护。第一层 Prompt 约束只能依据检索结果；第二层商品卡片携带结构化 evidence，包括真实 ID、价格、品牌；第三层 GroundingGuard 后校验，如果回答提到预算外价格、未提供的优惠、或没有引用任何候选商品，自动降级为模板回答。宁可保守，不编造。"

---

### Q10：多轮对话怎么保证上下文连贯？如果用户突然换话题呢？

**相关代码**：`server/session/state.py` (SessionState / merge_filters / reset_product_scope) <br> `server/agent/scope_transition.py` (scope_transition_policy) <br> `server/agent/context.py` (extract_contextual_filters / select_contextual_product)

> "会话状态 SessionState 保存 6 类信息：历史对话、正向过滤条件、排除条件、上一轮候选商品、购物车、澄清主题。当检测到用户从'跑鞋'换到'手机'，自动清理旧品类的预算和排除条件，防止污染。但对话历史保留，所以长上下文仍在。"

---

### Q11：图片找货是"真看懂"还是"假相似"？

**相关代码**：`server/inputs/multimodal.py` (MultimodalInputProcessor) <br> `server/inputs/image_similarity.py` (image_signature / signature_distance) <br> `server/inputs/visual_embedding.py` (cached_embed_image / cosine_similarity) <br> `server/inputs/base.py` (VLMProcessor 预留)

> "当前实现是视觉签名相似匹配，不是完整 VLM。但我们架构上预留了 VLMProcessor 接口，后续可以替换为真实视觉模型。当前方案的优势是无需 GPU、延迟低、可运行，适合演示验证。"

---

### Q12：这个项目和直接用 ChatGPT 有什么区别？

**相关代码**：`server/agent/recommendation_handler.py` (RecommendationHandler) <br> `server/rag/retrieval_pipeline.py` (ProductRetrievalPipeline) <br> `server/agent/grounding.py` (GroundingGuard) <br> `server/prompts/system_prompt.md`

> "直接用 ChatGPT 会编造商品和价格。我们的方案核心差异在于：RAG 检索严格约束回答范围、结构化后处理过滤、防幻觉三层校验。所有价格、品牌、库存来自真实数据，不是模型生成的。"

---

### Q13：如果让你继续优化，最先做哪个？

**相关代码**：`server/inputs/base.py` (VLMProcessor 预留) <br> `server/rag/retrieval_pipeline.py` (rerank_hits 替换点) <br> `scripts/evaluate_agent.py` (离线评估框架)

> "我会做两件事：一是把当前视觉签名升级为真实 VLM，实现真正'看图说话'；二是构建千级评估集，做自动化 A/B 测试。这两个方向在当前架构里都已预留接口，实现成本可控。"

---

### Q14：你们为什么用 embedding？和关键词搜索有什么区别？

**相关代码**：`server/rag/embeddings.py` (ArkEmbeddingFunction / HashingEmbeddingFunction) <br> `server/rag/stores.py` (ChromaStore.query) <br> `server/rag/scoring.py` (score_document_features / tokenize) <br> `server/rag/retrieval_pipeline.py` (ProductRetrievalPipeline.run)

> "Embedding 的核心目的是实现语义检索。传统关键词搜索只能匹配字面相同的词，比如用户搜'油皮'，如果商品标题里没有这个词就找不到。但 Embedding 把文本转成高维向量，能捕获语义关联——'油皮'和'控油'在向量空间里是接近的。
>
> 我们用了豆包的 doubao-embedding-vision，它是多模态模型，同时支持文本和图片的向量编码。这样用户输入文字或上传图片，都能进入同一个向量空间做相似度比较。"

---

### Q15：Embedding 的具体实现流程是什么？

**相关代码**：`server/rag/ingest.py` (灌库脚本) <br> `server/rag/embeddings.py` (build_chroma_embedding_function) <br> `server/rag/stores.py` (ChromaStore.add / query) <br> `server/inputs/visual_embedding.py` (build_product_visual_index)

> "灌库阶段：我们把 100 条商品数据读取后，每条生成文档，调用 embedding 模型把商品描述转成向量（768 维），存入 Chroma 向量数据库。同时图片也做同样处理，生成图片向量索引。
>
> 查询阶段：用户输入文本后，先转成同样的向量，在 Chroma 中做近似最近邻搜索（ANN），同时用 metadata 做预过滤（价格、库存等）。粗召回 50 条后，再经过 7 个 PostProcessor 过滤，最终输出 Top-5 给 LLM 生成回答。"

---

### Q16：没有 API Key 时怎么跑？

**相关代码**：`server/rag/embeddings.py` (HashingEmbeddingFunction) <br> `server/rag/stores.py` (LocalJsonVectorStore) <br> `server/rag/vector_store.py` (VectorStore 抽象) <br> `server/config.py` (use_ark_embedding / use_chroma 开关)

> "我们做了两套 embedding 实现。有 API Key 时，调用豆包的 doubao-embedding-vision 真实模型，语义理解能力强。没有 API Key 时，自动 fallback 到 HashingEmbeddingFunction，用本地哈希算法把文本转成 384 维向量，不需要网络、不需要费用，保证项目能直接运行。"

---

### Q17：向量相似度比较用的是哪种方式？重排序用的是哪种方式？

**相关代码**：`server/rag/stores.py` (ChromaStore.query / LocalJsonVectorStore.query) <br> `server/rag/scoring.py` (score_chroma_hit / score_document_features) <br> `server/rag/retrieval_pipeline.py` (rerank_hits / relevance_bonus) <br> `server/rag/vector_store.py` (VectorStore 抽象)

> "向量相似度比较分两个场景。有 API Key 时，Chroma 内部用 HNSW 近似最近邻算法，返回 distance 值，代码里转成 1.0/(1.0+distance) 作为相似度分。无 API Key 的 fallback 模式，用关键词重叠计数（token overlap + phrase bonus + title bonus）来打分，简单说就是数一下用户查询的词和商品文档有多少词重合，重合越多分越高。
>
> 重排序我们用的是规则化加权重排序，不是 BGE-Reranker 等神经网络模型。每条候选先算混合分（向量相似度分 + 关键词匹配分），然后按结构化匹配程度逐项加分：商品类型精确匹配 +20、大类 +12、关键词 +6、品牌 +5、预算内 +2、用户直接提到商品名 +8。这样结构化匹配优先于关键词匹配，可控性更强。100 条数据规模下规则加权足够有效，万级数据时可以在当前架构中接入外部 reranker，只替换 Pipeline 的 rerank 阶段，其他层不动。"

---

## 五、响应速度优化详解

### 5.1 即时 token（让用户"感觉快"）—— 感知优化

**策略**：在 LLM 还在思考、检索还在跑的时候，先给用户发一个"正在为您搜索…"，不是白屏等待。

**代码逻辑**（`server/agent/recommendation_handler.py`）：

```python
async def recommendation_handler(turn: AgentTurnContext, ...):
    if not needs_search:
        answer = build_grounded_answer(message, [], intent)
        async for item in stream_text(answer):
            yield item
        yield {"event": "done", ...}
        return
    
    # 核心优化点：如果走了 LLM 路径，先推送一个即时回复，降低等待感
    if turn.recommendation_llm_budget_seconds > 0:
        yield {"event": "token", "data": {"text": "正在为您搜索相关商品…"}}
    
    # 同时后台执行检索和 LLM 生成
    search_task = asyncio.create_task(
        execute_product_search_with_budget(...)
    )
    ...
```

**效果**：用户看到的是"正在为您搜索…"几个字立刻出现，而不是 0.3-1 秒的白屏。

**相关代码**：`server/agent/recommendation_handler.py` (recommendation_handler / stream_text) <br> `server/agent/responses.py` (stream_text / iter_response_chunks)

---

### 5.2 SSE 流式传输（边生成边渲染）—— 传输优化

**策略**：不用等后端全部生成完再一次性返回，而是生成一个 token 就推一个 token，前端边收边渲染。

**代码逻辑**（`server/api/chat.py`）：

```python
async def event_stream(...):
    async for event in orchestrator.stream_chat(...):
        yield f"event: {event['event']}\ndata: {json.dumps(event['data'])}\n\n"
```

**前端**（`client/.../ChatSseClient.kt`）逐字渲染，仿打字机效果。

**效果**：用户看到文字是逐字出现的，即使总耗时 1 秒，但第一个字在 300ms 就出现，感知延迟大幅降低。

**相关代码**：`server/api/chat.py` (event_stream / StreamingResponse) <br> `client/app/src/.../network/ChatSseClient.kt` (Android SSE 客户端)

---

### 5.3 Embedding 缓存（避免重复计算）—— 计算复用

**策略**：同一个查询文本或同一张图片，第二次查询时直接读 SQLite 缓存，不调用 API。

**代码逻辑**（`server/rag/embedding_cache.py`）：

```python
class EmbeddingCache:
    def get(self, key: EmbeddingCacheKey) -> list[float] | None:
        # 查 SQLite：SELECT vector_json FROM embeddings WHERE cache_key = ?
        row = connection.execute(
            "SELECT vector_json FROM embeddings WHERE cache_key = ?",
            (key.storage_key,)
        ).fetchone()
        return [float(v) for v in json.loads(row[0])] if row else None
```

**缓存键**包含 provider + model + modality + content_hash，换模型自动隔离。

**效果**：
- 首次调用：HTTP 网络请求 → 几百毫秒
- 后续调用：SQLite 本地读取 → 几毫秒

**图片检索也复用**（`server/inputs/visual_embedding.py`）：

```python
def cached_embed_image(embedder, cache, image_bytes, mime_type):
    prepared_bytes, _ = prepare_image_for_embedding(image_bytes, mime_type)
    content_hash = hashlib.sha256(prepared_bytes).hexdigest()
    cache_key = EmbeddingCacheKey(...)
    cached = cache.get(cache_key)
    if cached is not None:
        return cached  # 命中缓存，直接返回
    vector = embedder.embed_image_bytes(...)  # 未命中才调用 API
    cache.set(cache_key, vector)
    return vector
```

**相关代码**：`server/rag/embedding_cache.py` (EmbeddingCache) <br> `server/inputs/visual_embedding.py` (cached_embed_image)

---

### 5.4 短预算超时 + 自动降级（防止拖死）—— 兜底优化

**策略**：每个 LLM 调用都有严格的超时预算，超时不会卡死，而是自动降级到本地规则/模板。

#### 1. 语义规划层（SemanticPlanner）

**代码**（`server/agent/semantic_llm.py`）：

```python
async def _collect_llm_plan_tokens(self, message, session):
    async with asyncio.timeout(self.timeout_seconds):  # 默认 0.8 秒！
        async for token in client.stream_messages(...):
            chunks.append(token)
    return chunks
```

**超时后果**：

```python
async def _try_llm_plan(self, message, session):
    try:
        chunks = await self._collect_llm_plan_tokens(...)
        ...
    except (TimeoutError, ValidationError, json.JSONDecodeError):
        return None  # 静默 fallback，不报错
```

→ 超时后返回 `None`，上层自动用**规则层 fallback**。

#### 2. LLM 生成回答层（RecommendationHandler）

**代码**（`server/agent/recommendation_handler.py`）：

```python
async def collect_llm_answer_with_budget(context, cards):
    budget = float(context.recommendation_llm_budget_seconds or 0.0)
    if budget <= 0:
        return ""  # 不调用 LLM，直接模板
    
    task = asyncio.create_task(collect_llm_answer(context, cards))
    try:
        return await asyncio.wait_for(task, timeout=budget)
    except TimeoutError:
        task.cancel()
        return ""  # 超时返回空字符串，上层用模板回答
```

#### 3. 检索层

**代码**（`server/agent/recommendation_handler.py`）：

```python
async def execute_product_search_with_budget(..., timeout_seconds: float):
    try:
        return await asyncio.wait_for(
            asyncio.to_thread(run_search), 
            timeout=timeout_seconds
        )
    except TimeoutError:
        return ProductSearchExecution(cards=[], diagnostics=[])
```

**相关代码**：`server/agent/semantic_llm.py` (SemanticPlanner._try_llm_plan / timeout_seconds) <br> `server/agent/recommendation_handler.py` (collect_llm_answer_with_budget / execute_product_search_with_budget) <br> `server/gateway/resilience.py` (run_with_timeout)

---

### 5.5 本地 fallback（无网络也能跑）—— 可用性优化

**策略**：没有 API Key、网络不通、Chroma 未安装时，全部走本地计算，零网络延迟。

#### 1. Embedding 本地 fallback

**代码**（`server/rag/embeddings.py`）：

```python
def build_chroma_embedding_function(...):
    if use_ark_embedding and api_key:
        return ArkEmbeddingFunction()  # 网络调用，几百毫秒
    return HashingEmbeddingFunction(dimensions=384)  # 本地计算，毫秒级
```

#### 2. 向量数据库本地 fallback

**代码**（`server/rag/stores.py` `LocalJsonVectorStore`）：

```python
def query(self, query: str, top_k=5, filters=None):
    query_tokens = tokenize(query)
    hits = []
    for doc in self.candidate_documents(filters):
        score = score_document_features(query_tokens, self.document_features[doc.id])
        if score > 0:
            hits.append(...)
    return heapq.nlargest(top_k, hits, key=lambda x: x["score"])
```

→ 纯内存倒排索引 + 关键词打分，**P95 16-130ms**（benchmark 数据）。

**相关代码**：`server/rag/embeddings.py` (HashingEmbeddingFunction) <br> `server/rag/stores.py` (LocalJsonVectorStore) <br> `server/rag/vector_store.py` (VectorStore 抽象)

---

### 5.6 Metadata 预过滤（减少向量搜索空间）—— 检索优化

**策略**：向量搜索前，先用结构化条件（价格、库存、类目）在数据库层面过滤，减少向量比对数量。

**代码**（`server/rag/stores.py` `ChromaStore.query`）：

```python
def query(self, query: str, filters: SearchFilters, top_k: int = 5):
    query_embedding = self.embedding_function([query])[0]
    where_clause = build_chroma_where(filters)  # 预过滤！
    
    results = self.collection.query(
        query_embeddings=[query_embedding],
        n_results=top_k * 10,  # 粗召回 50 条
        where=where_clause,     # 预过滤：price<=200, stock>0
    )
```

**效果**：如果用户要求"预算 200 以内"，Chroma 内部只会在 `price <= 200` 的商品上做向量比对，而不是全量 100 条。

**相关代码**：`server/rag/stores.py` (ChromaStore.query) <br> `server/rag/chroma_metadata.py` (build_chroma_where)

---

### 5.7 异步并发（非阻塞架构）—— 架构优化

**策略**：FastAPI + asyncio，所有 IO 操作（网络请求、数据库查询、文件读取）都是非阻塞的。

**代码**（`server/llm/ark_client.py`）：

```python
async def _stream_messages_once(self, messages):
    async with httpx.AsyncClient() as client:
        async with client.stream(
            "POST", url, json=payload, headers=headers, timeout=timeout
        ) as response:
            async for line in response.aiter_lines():
                if line.startswith("data: "):
                    yield parse_chunk(line)
```

→ `async/await` + `httpx.AsyncClient`，不是同步 `requests`，不会阻塞其他请求。

**代码**（`server/api/chat.py`）：

```python
@app.post("/chat")
async def chat(...):
    return StreamingResponse(
        event_stream(...),
        media_type="text/event-stream",
    )
```

→ FastAPI 原生 async，一个请求等 LLM 时，其他请求照样处理。

**相关代码**：`server/llm/ark_client.py` (ArkChatClient._stream_messages_once) <br> `server/api/chat.py` (POST /chat / StreamingResponse) <br> `server/gateway/resilience.py` (CircuitBreaker / retry_async_operation)

---

### 5.8 总结：7 层响应速度优化

| 层级 | 策略 | 代码位置 | 效果 |
|------|------|----------|------|
| **1. 感知层** | 即时 token | `recommendation_handler.py` | 用户看到文字立刻出现，降低等待焦虑 |
| **2. 传输层** | SSE 流式推送 | `api/chat.py` + Android SSE | 边生成边渲染，首字延迟 300ms |
| **3. 缓存层** | Embedding 缓存 | `embedding_cache.py` | 重复查询从几百毫秒降到几毫秒 |
| **4. 超时层** | 短预算 + 自动降级 | `semantic_llm.py` / `recommendation_handler.py` | 0.8s 超时回退规则，不卡死 |
| **5. 可用层** | 本地 fallback | `embeddings.py` / `stores.py` | 无网络也能跑，P95 < 130ms |
| **6. 检索层** | Metadata 预过滤 | `stores.py` `build_chroma_where` | 减少向量比对数量 |
| **7. 架构层** | 异步非阻塞 | `ark_client.py` / `main.py` | FastAPI + asyncio，并发不阻塞 |

**压测数据**：
- 50K 本地检索：P95 16-130ms
- Chroma 1K 检索：P95 33-178ms
- 首 Token 延迟：P95 318-342ms（< 1s 阈值）

**答辩金句**：
> "响应速度优化不是单点优化，而是分层设计。第一层让用户感觉快——即时 token + SSE 流式；第二层让计算快——embedding 缓存 + 本地 fallback；第三层防止拖慢——0.8 秒短预算超时自动降级；第四层让检索快——metadata 预过滤减少向量搜索空间；第五层架构支撑——FastAPI + asyncio 原生异步。实测首 token P95 318ms，低于 1 秒阈值。"

---

## 六、关键代码定位速查

| 如果想看... | 打开这个文件 |
|------------|-------------|
| 后端怎么启动 | `server/main.py` |
| 一次请求怎么走的 | `server/agent/orchestrator.py` |
| 怎么防幻觉 | `server/agent/grounding.py` |
| 怎么分派不同请求 | `server/agent/workflow.py` |
| 购物车怎么实现 | `server/tools/cart.py` + `server/tools/cart_operations.py` |
| 怎么找相似商品 | `server/rag/retrieval_pipeline.py` |
| 系统提示词写了什么 | `server/prompts/system_prompt.md` |
| 客户端界面 | `client/app/src/.../ui/ChatScreen.kt` |
| 图片怎么处理 | `server/inputs/multimodal.py` + `server/inputs/visual_embedding.py` |
| 会话状态定义 | `server/session/state.py` |
| 语义规划双层 | `server/agent/semantic_llm.py` + `server/agent/semantic_rules.py` |
| 商品对比 | `server/agent/commerce_handlers.py` CompareHandler |
| 场景组合 | `server/agent/commerce_handlers.py` ScenarioBundleHandler |
| 上下文追问 | `server/agent/context.py` + `server/agent/conversation_handlers.py` |
| 检索过滤 | `server/rag/post_process.py` |
| 向量存储 | `server/rag/vector_store.py` |
| embedding 实现 | `server/rag/embeddings.py` |
| 灌库脚本 | `server/rag/ingest.py` |
| **响应速度优化** | `server/agent/recommendation_handler.py` (即时 token) <br> `server/api/chat.py` (SSE 流式) <br> `server/rag/embedding_cache.py` (缓存) <br> `server/gateway/resilience.py` (超时/降级) <br> `server/rag/stores.py` (预过滤) <br> `server/llm/ark_client.py` (异步) |
| 测试集合 | `server/tests/`（35+ 文件） |
| 离线评估 | `scripts/evaluate_agent.py` + `data/eval_queries.jsonl` |
| 压测脚本 | `scripts/benchmark_retrieval.py` + `scripts/benchmark_first_token.py` |
| 配置管理 | `server/config.py` + `.env.example` |
| 架构文档 | `docs/architecture.md` |
| 启动指南 | `启动指南.md` |

---

## 七、答辩策略建议

### 时间分配（25 分钟）

- 开场（背景+目标）：2 分钟
- 技术铺垫（选型+架构+数据流）：2 分钟
- **功能演示（6 大场景）**：8 分钟（重点！）
- **技术深度（RAG/Planner/Handler/防幻觉/会话状态/多模态）**：6 分钟（重点！）
- 工程规范（抽象接口+测试+性能）：3 分钟
- 收尾（成果+收获+未来）：4 分钟

### 亮点突出

- **多轮对话 + 反选排除**：ExclusionFilter 程序化过滤，不是让 AI"理解"
- **上下文追问**：ordinal/name/brand/price 四种引用类型，全部走结构化解析
- **对话式购物车**：模糊购买进入确认模式，防止误操作
- **商品对比**：分别检索两侧，避免 Top-K 只召回一个品牌
- **场景组合**：新增场景只需改 JSON 配置，10 分钟扩展
穷举法

| 场景 ID                   | 触发词                     | 槽位（Slots）               | 说明      |
| ----------------------- | ----------------------- | ----------------------- | ------- |
| `sanya_trip`            | 三亚、马尔代夫、巴厘岛、普吉、海边、度假、旅游 | 防晒保护、轻便上衣、舒适出行、拍照记录（可选） | 海岛度假组合  |
| `commute`               | 通勤、上班、办公室               | 通勤背包、降噪耳机、轻办公设备（可选）     | 日常通勤组合  |
| `sports_training`       | 健身、跑步装备、运动套装            | 训练跑鞋、运动裤、速干上衣、运动耳机（可选）  | 运动训练组合  |
| `cross_category_bundle` | 搭配一套、组合推荐、一套方案          | 基础护理、日常穿搭、随身数码          | 跨类目通用组合 |

- **防幻觉**：三层防护，宁可保守降级，不编造价格/优惠

### 未实现项应对

| 未实现 | 答辩话术 |
|--------|----------|
| 语音输入 | "未实现，但架构已预留 ASRProcessor 接口，后续接入成本可控" |
| TTS | "未实现，架构预留了 TTS 扩展点" |

### 部分实现项应对

| 部分实现 | 答辩话术 |
|----------|----------|
| 数据一致性 | "当前为静态演示数据，但 VectorStore/FactProvider 抽象层已预留，生产环境可替换为实时数据源" |
| 端侧体验打磨 | "流式渲染和商品卡片交互已具备，骨架屏和复杂动画是后续优化方向" |
| 下单确认 | "模拟下单闭环已完成（地址确认+订单汇总），真实支付链路需外部系统接入" |

### 最高频追问组合（背熟这 3 个）

1. **session/state.py**（多轮怎么做的）
2. **rag/vector_store.py**（怎么扩展）
3. **conversation_handlers.py**（追问怎么实现的）

这三个背熟了，答辩技术追问基本能过关。

---

> 本文档不含 PPT 页码对应和逐页讲稿。如需 PPT 制作内容，请另行查阅。
