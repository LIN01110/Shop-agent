# RAG 多模态电商智能导购 Agent —— 完整启动流程（中文版）

---

## 前置条件

以下环境已假设准备完成：
- conda 已安装，且存在名为 `smart_agent` 的环境
- 手机已开启 USB 调试，并通过 USB 线连接电脑
- Android Studio 已安装并可正常运行
- 项目路径：`C:\Users\58380\Desktop\Shop_Agent\rag-shopping-agent-main`

---

## 步骤一：启动后端服务器（PowerShell）

1. 打开 PowerShell，进入项目目录：

```powershell
cd "C:\Users\58380\Desktop\Shop_Agent\rag-shopping-agent-main"
```

2. 激活 conda 环境：

```powershell
conda activate smart_agent
```

3. 安装/更新依赖（首次或 requirements.txt 变更后执行）：

```powershell
pip install -r server\requirements.txt
```

4. 检查 `.env` 文件（仅首次需要，确保 `ENABLE_DEBUG_API` 有值）：

打开 `.env` 文件，确认第 5 行不是空的：

```env
ENABLE_DEBUG_API=true
```

如果为空，补上 `true` 或 `false`。

5. 启动后端服务器：

```powershell
.\scripts\run_server.ps1
```

或手动启动：

```powershell
$env:PYTHONPATH = (Get-Location).Path
python -m uvicorn server.main:app --host 127.0.0.1 --port 8000 --reload
```

⚠️ **重要**：此窗口必须保持运行，不要关闭！最小化即可。

---

## 步骤二：验证后端是否正常运行（另开 PowerShell）

1. 新开一个 PowerShell 窗口，执行健康检查：

```powershell
curl http://127.0.0.1:8000/health
```

期望返回：

```json
{"status":"ok"}
```

---

## 步骤三：设置 USB 端口转发（同一窗口）

1. 确保手机已用 USB 线连接电脑，且已开启 USB 调试。

2. 设置 adb 反向代理：

```powershell
adb reverse tcp:8000 tcp:8000
```

3. 验证转发是否生效：

```powershell
adb reverse --list
```

期望输出：

```
tcp:8000 tcp:8000
```

---

## 步骤四：在 Android Studio 中编译并运行（Android Studio）

1. 打开 Android Studio。

2. 点击 **File → Open**，选择文件夹：

```
C:\Users\58380\Desktop\Shop_Agent\rag-shopping-agent-main\client
```

3. 等待 Gradle Sync 完成（首次可能较慢，请耐心等待）。

4. 在顶部工具栏的设备下拉框中，选择已连接的真机（如 `Xiaomi 22041211AC`）。
   - 如果没有显示，请检查 USB 调试是否开启，并执行：`adb devices`

5. 点击绿色 **▶️ Run** 按钮（或按 `Shift + F10`）。

---

## 步骤五：处理签名冲突（如弹出安装失败对话框）

如果弹出提示：

> Application Installation Failed — The device already has an application with the same package but a different signature.

**方式 A（直接替换，推荐）**：

点击对话框中的 **OK**，自动卸载旧版并安装新版。

**方式 B（保留旧版，两个版本共存）**：

1. 在 Android Studio 中打开文件：`client/app/build.gradle.kts`
2. 找到第 12 行：

```kotlin
applicationId = "com.example.ragshoppingagent"
```

3. 修改为新的包名，例如：

```kotlin
applicationId = "com.example.ragshoppingagent.dev"
```

4. 点击 **▶️ Run** 重新编译，新旧版本将同时存在于手机。

---

## 步骤六：在手机上测试对话

App 安装成功后，打开应用，输入测试消息：

```
推荐一款适合油皮的洗面奶，预算100以内
```

观察：
- ✓ 是否有流式回复（文字逐字出现）
- ✓ 是否出现商品卡片（带图片、价格、名称）

---

## 快速命令速查表

| 目的 | PowerShell 命令 |
|------|----------------|
| 进入项目目录 | `cd "C:\Users\58380\Desktop\Shop_Agent\rag-shopping-agent-main"` |
| 激活 conda 环境 | `conda activate smart_agent` |
| 安装依赖 | `pip install -r server\requirements.txt` |
| 启动后端 | `.\scripts\run_server.ps1` |
| 健康检查 | `curl http://127.0.0.1:8000/health` |
| 设置 adb 反向代理 | `adb reverse tcp:8000 tcp:8000` |
| 查看 adb 转发规则 | `adb reverse --list` |
| 查看连接设备 | `adb devices` |
| 手动卸载旧版 App | `adb uninstall com.example.ragshoppingagent` |
| 停止后端服务器 | 在服务器窗口按 `Ctrl + C` |

---

## 可选配置（根据需要启用）

**启用 LLM 生成回答**（需要 `.env` 中配置 `ARK_API_KEY`）：

```powershell
$env:USE_LLM = "true"
.\scripts\run_server.ps1
```

**启用 Chroma 向量检索**：

```powershell
$env:USE_CHROMA = "true"
.\scripts\run_server.ps1
```

**启用语义规划**（默认关闭，保证演示延迟稳定）：

```powershell
$env:USE_SEMANTIC_LLM = "true"
.\scripts\run_server.ps1
```

---

## 常见问题

**Q1: 后端启动报错 "enable_debug_api Input should be a valid boolean"**

> A: 打开 `.env` 文件，把 `ENABLE_DEBUG_API=` 改为 `ENABLE_DEBUG_API=true`

**Q2: 手机上 App 无法连接后端**

> A: 检查 (1) 后端是否运行 (2) `adb reverse` 是否设置 (3) 手机和电脑是否连同一 WiFi

**Q3: Android Studio 真机列表为空**

> A: 检查手机 USB 调试是否开启，尝试更换 USB 线或 USB 口，执行 `adb kill-server` 后 `adb start-server`

---
