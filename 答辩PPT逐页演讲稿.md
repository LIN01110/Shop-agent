# RAG 多模态电商智能导购 Agent — 26页PPT逐页演讲稿

> **使用方法**：每页控制在 20-30 秒，语速适中，总时长约 25 分钟。关键数字和术语可提前标注。括号内为备注，不需要读出。

---

## 第 1 页：封面

> 标题：RAG 多模态电商智能导购 AI Agent  
> 副标题：基于 RAG 与 LLM 的多轮对话商品推荐系统

**演讲稿**：
> "老师好，各位同学好。今天我答辩的题目是《RAG 多模态电商智能导购 AI Agent》。这个项目的目标是构建一个能听懂多轮对话、能看图找货、能防幻觉的商品推荐系统。"

---

## 第 2 页：项目背景与问题

> 痛点：传统搜索只能匹配关键词，不能理解语义；大模型直接回答会编造价格；电商场景需要多轮、排除、对比、购物车

**演讲稿**：
> "电商导购有两个核心痛点。第一，传统搜索是关键词匹配，用户说'油皮洗面奶'，如果标题没有'油皮'就找不到。第二，直接用大模型回答，会编造价格、编造优惠，这是电商场景不可接受的。我们的方案是 RAG + LLM 的双层架构：检索保证事实准确，LLM 保证自然表达。"

---

## 第 3 页：核心目标与技术选型

> 6 大核心能力 + 技术栈（Android/Kotlin + FastAPI + Chroma + 豆包）

**演讲稿**：
> "我们定义了 6 大核心目标：多轮对话、反选排除、上下文追问、图片找货、场景组合、商品对比。技术选型上，客户端用 Android Jetpack Compose 原生开发，后端用 FastAPI + Python，向量数据库用 Chroma，大模型用豆包 doubao-vision-lite，数据规模是 100 条商品、4 个类目、100 张主图。"

---

## 第 4 页：系统架构全景

> 一张大图：Android 客户端 ↔ FastAPI 服务端 ↔ Agent 核心 ↔ RAG 引擎 ↔ LLM

**演讲稿**：
> "这是系统架构全景。用户从 Android 端发起对话，通过 SSE 流式长连接到达 FastAPI 服务端。后端的核心是 Agent 编排层，包括意图识别、多轮状态管理、Handler 路由。下面是 RAG 引擎，负责检索召回、过滤、重排。最底层是 LLM，用于生成自然语言回答。整个架构是分层、可扩展的。"

---

## 第 5 页：数据工程与特征治理

> 100 商品 / 4 类目 / 向量化 / Chunking 策略

**演讲稿**：
> "数据工程方面，我们构建了 100 条商品数据，覆盖美妆、服饰、食品、数码 4 个类目。每条商品向量化时采用'一商品一主块+一详情块'的 Chunking 策略。结构化字段如价格、库存单独存 metadata，不进入向量，保证过滤精确。同时灌了 100 张商品主图，支持图片检索。"

---

## 第 6 页：完整消息处理流程（13 步）

> 从输入到商品卡片的 13 步流程图

**演讲稿**：
> "一次推荐请求要走 13 步。简要说：输入处理 → 语义规划 → 上下文提取 → 品类切换判断 → 过滤条件合并 → Agent 路由 → 检索召回 → 答案生成 → 防幻觉校验 → 流式输出。每一步都有明确的职责和降级策略。"

---

## 第 7 页：语义规划双层设计

> 规则层（0.01ms）+ LLM 层（0.8s 预算）+ 合并逻辑

**演讲稿**：
> "语义规划是核心创新。我们采用双层设计：第一层是规则层，用关键词、正则、词表快速匹配，零延迟、高确定性。第二层是 LLM 层，预算 0.8 秒，超时自动回退规则层。如果 LLM 结果可信，就与规则层合并；如果不可信或超时，直接丢弃。"

---

## 第 8 页：意图路由与 6 大 Handler

> Clarification → Cart → Compare → Bundle → FollowUp → Recommendation（兜底）

**演讲稿**：
> "Handler 路由按优先级排序：先判断是否需要澄清，再判断购物车操作，再判断对比，再判断场景组合，再判断上下文追问，最后兜底推荐。每个 Handler 实现统一的 matches + handle 接口，新增 Handler 只需注册到列表，编排逻辑不用改。"

---

## 第 9 页：RAG 检索引擎（5 阶段）

> 粗召回 → 7 层过滤 → 去重 → 重排 → Top-K 截断

**演讲稿**：
> "RAG 检索分 5 阶段。第一阶段粗召回 50 条候选。第二阶段到第七阶段是 7 个 PostProcessor：价格、类型、类目、品牌、库存、关键词、排除，程序化过滤。第八阶段去重。第九阶段重排，按结构化匹配加分，比如商品类型精确匹配加 20 分，预算内加 2 分。最终输出 Top 5。"

---

## 第 10 页：PostProcessor 7 层过滤

> Range / Type / Category / Brand / Stock / Keyword / Exclusion

**演讲稿**：
> "7 个 PostProcessor 都是程序化过滤，不依赖 AI 理解。比如排除过滤器：用户说'不要耐克'，系统直接用品牌别名表排除，不是让 LLM 判断。这样稳定、可测试、可解释。新增过滤器只需实现 apply 方法，插入 Pipeline 即可。"

---

## 第 11 页：多轮对话与 Session 状态

> 6 核心字段：history / filters / exclusions / candidates / cart / pending_subject

**演讲稿**：
> "多轮对话依赖 SessionState 的 6 个核心字段：对话历史、正向过滤条件、排除条件、上一轮候选商品、购物车、待澄清主题。过滤条件是多轮累积的，比如第一轮说'预算 200'，第二轮说'不要耐克'，两个条件同时生效。但品类切换时会自动清理旧条件，防止'眼霜的预算'带到'手机'搜索里。"

---

## 第 12 页：上下文追问（4 种引用类型）

> 第 N 个 / 商品名 / 品牌名 / 价格区间

**演讲稿**：
> "上下文追问支持 4 种引用类型：说'第二个怎么样'，按索引取；说'科颜氏那款'，按品牌名取；说'便宜的那个'，按价格区间取；说'刚才推荐的'，按上一轮候选取。全部走结构化解析，不是让 LLM 猜。"

---

## 第 13 页：防幻觉三层机制

> Prompt 约束 → Evidence 绑定 → GroundingGuard 后校验（4 条规则）

**演讲稿**：
> "防幻觉是我们最重视的工程点。三层防护：第一层系统提示词约束只能依据候选商品；第二层商品卡片携带结构化 evidence，包含真实 ID、价格、品牌；第三层 GroundingGuard 后校验，如果回答提到预算外价格、未提供的优惠、或没有引用任何候选商品，自动降级为模板回答。宁可保守，不编造。"

---

## 第 14 页：多模态输入与图片找货

> 视觉签名（12×12 RGB）+ 多模态 Embedding（豆包 768 维）

**演讲稿**：
> "图片找货支持双方案。有 API Key 时，用豆包多模态模型把图片转成 768 维向量，和商品库图片做余弦相似度。无 API Key 时，fallback 到视觉签名：把图片缩到 12 乘 12 像素，提取 RGB 值，得到 432 维向量，算欧氏距离。两套方案都在架构中预留，无缝切换。"

---

## 第 15 页：商品对比

> 两侧独立检索 + 结构化对比面板 + 推荐结论

**演讲稿**：
> "商品对比不是简单的并排展示。系统先解析用户说的两个品牌，分别检索两侧，强制保证双方都有候选。然后生成结构化对比面板：价格、容量、成分、适用场景逐一对比。最后给出推荐结论，基于结构化数据，不是让 LLM 自由发挥。"

---

## 第 16 页：场景组合推荐

> 4 个模板（三亚/通勤/健身/通用）+ 配置驱动 + 穷举打分

**演讲稿**：
> "场景组合支持 4 个模板：三亚度假、日常通勤、健身训练、跨类目通用。用户说'去三亚帮我配一套'，系统匹配到三亚模板，包含防晒、上衣、出行、拍照 4 个 slot。每个 slot 独立检索，然后穷举所有组合，按预算打分，输出最优组合。新增场景只需改 JSON 配置，不用改代码。"

---

## 第 17 页：对话式购物车

> 自然语言操作 + 复合指令 + 确认机制

**演讲稿**：
> "购物车支持自然语言操作：'把第一个加到购物车''删除第二个''数量改成 2'。还支持复合指令，比如'删除第一个，然后把第二个加到购物车'，系统会按顺序解析为多个操作。购买操作有确认机制，用户说'买两双'，系统不会直接执行，而是先问'确认加入购物车吗？'，防止误操作。"

---

## 第 18 页：客户端工程（Android）

> Jetpack Compose + MVVM + SSE 流式渲染 + 打字机效果

**演讲稿**：
> "客户端用 Android 原生 Kotlin 开发，UI 框架是 Jetpack Compose，架构是 MVVM。网络层用 OkHttp 建立 SSE 长连接，ViewModel 解析 6 种事件类型：token 逐字渲染打字机效果，product_card 渲染商品卡片，cart_update 展开购物车面板。整个 UI 是声明式的，响应数据驱动。"

---

## 第 19 页：性能优化（7 层策略）

> 即时 token / SSE 流式 / Embedding 缓存 / 短预算超时 / 本地 fallback / 预过滤 / 异步架构

**演讲稿**：
> "响应速度优化不是单点，而是 7 层分层设计。感知层：即时 token 降低等待焦虑。传输层：SSE 流式，首字 300 毫秒出现。缓存层：Embedding 重复查询从几百毫秒降到几毫秒。超时层：0.8 秒短预算自动降级。可用层：无网络也能跑本地 fallback。检索层：metadata 预过滤减少向量比对。架构层：FastAPI + asyncio 原生异步。"

---

## 第 20 页：测试体系（40+ 测试）

> 单元测试 + 离线评估 + 性能压测 + 调试追踪

**演讲稿**：
> "测试体系分三层。单元测试 40 多条，用 pytest + FastAPI TestClient，覆盖 API、Agent、RAG、购物车全链路。离线评估用 JSONL 用例自动执行，检查 handler 路由、商品命中、文本包含。性能压测分检索压测和首 token 压测，验证 P95 延迟。还有 debug/traces 实时链路追踪，生产环境可排障。"

---

## 第 21 页：工程深度（6 大抽象接口）

> VectorStore / ToolRegistry / InputProcessor / PostProcessor / SSE 协议 / SessionStore

**演讲稿**：
> "工程深度体现在 6 大抽象接口。VectorStore 让 Chroma、本地 JSON、未来 Redis 无缝切换。ToolRegistry 让新增工具只注册不动编排。InputProcessor 预留了语音和 VLM 扩展。PostProcessor 让新增过滤器即插即用。SSE 事件协议保证前后端兼容。SessionStore 让内存、SQLite、Redis 可切换。每个接口都遵循定义接口、实现具体类、工厂注入的模式。"

---

## 第 22 页：压测数据与结果

> 50K 本地 P95 130ms / Chroma 1K P95 178ms / 首 Token P95 342ms（< 1s）

**演讲稿**：
> "压测数据：50K 本地检索 P95 130 毫秒，Chroma 1K 检索 P95 178 毫秒，首 token 延迟 P95 342 毫秒，全部低于 1 秒阈值。这些数据证明了分层优化策略的有效性，也证明了系统从 100 条到 50K 条数据的可扩展性。"

---

## 第 23 页：项目成果总结

> 已实现 22 项 / 部分实现 5 项 / 未实现 2 项（语音、TTS）

**演讲稿**：
> "项目成果总结：已实现 22 项核心功能，包括多轮对话、反选排除、上下文追问、图片找货、场景组合、商品对比、对话式购物车、防幻觉三层、流式渲染等。部分实现 5 项，如端侧体验打磨。未实现 2 项：语音输入和 TTS，但架构已预留接口，后续接入成本可控。"

---

## 第 24 页：已知未实现与应对

> 语音输入（ASRProcessor 预留）/ TTS（架构预留）/ 真实库存（VectorStore 接口预留）

**演讲稿**：
> "对于未实现项，我们的应对策略是诚实说明 + 架构预留。语音输入未实现，但 InputProcessor 已预留 ASRProcessor 接口。TTS 未实现，但 SSE 流式架构天然支持。真实库存未接入，但 FactProvider 抽象层已预留。这说明从 MVP 到生产的路径是清晰的。"

---

## 第 25 页：个人收获与总结

> 工程思维：分层设计 / 接口抽象 / 测试驱动 / 性能优化 / 防幻觉工程化

**演讲稿**：
> "个人收获方面，我学到了五点工程思维。第一，分层设计：复杂系统必须分层，每层有明确职责。第二，接口抽象：面向接口编程，让系统可扩展。第三，测试驱动：用 JSONL 用例做回归测试，保证改代码不退化。第四，性能优化：不是单点优化，而是感知、传输、缓存、超时、可用、检索、架构 7 层。第五，防幻觉工程化：不是仅靠提示词，而是 Prompt + Evidence + Guard 三层硬约束。"

---

## 第 26 页：致谢

> 感谢导师、感谢团队、感谢评审

**演讲稿**：
> "最后，感谢导师的指导，感谢团队成员的支持，感谢各位评审老师的宝贵时间。以上就是我的答辩内容，请老师批评指正。"

---

## 附：时间分配建议（25 分钟总答辩）

| 阶段 | 页码 | 时长 | 说明 |
|---|---|---|---|
| 开场 | 1-3 | 2 分钟 | 背景、目标、选型 |
| 架构与核心 | 4-13 | 6 分钟 | 13 步流程、双层规划、RAG、多轮、防幻觉 |
| 功能演示 | （实机） | 5 分钟 | 见下方实机演示流程 |
| 特色功能 | 14-17 | 3 分钟 | 图搜、对比、组合、购物车 |
| 工程规范 | 18-22 | 4 分钟 | 客户端、性能、测试、抽象接口、压测 |
| 总结与收尾 | 23-26 | 3 分钟 | 成果、未实现、收获、致谢 |
| 问答 | — | 2 分钟 | 预留 |

**合计：25 分钟**

---

## 附：关键数字速记卡

| 数字 | 含义 | 答辩金句 |
|---|---|---|
| 13 步 | 完整消息处理流程 | "从输入到商品卡片，要走 13 步，每一步都有降级策略" |
| 0.8 秒 | LLM 规划超时 | "语义规划 LLM 层预算 0.8 秒，超时自动回退规则层" |
| 50 条 | 粗召回数量 | "先粗召回 50 条，再经过 7 层过滤，最终输出 Top 5" |
| 7 个 | PostProcessor 数量 | "7 个程序化过滤器，稳定、可测试、可解释" |
| 6 个 | SessionState 字段 | "会话状态 6 个核心字段，支撑多轮对话和上下文追问" |
| 3 层 | 防幻觉机制 | "Prompt + Evidence + Guard 三层防幻觉，宁可保守不编造" |
| 4 种 | 引用类型 | "上下文追问支持 4 种引用：索引、品牌、价格、上一轮" |
| 342 毫秒 | 首 Token P95 | "首 token P95 延迟 342 毫秒，低于 1 秒阈值" |
| 40+ | 测试用例数 | "40 多条单元测试，覆盖 API、Agent、RAG、购物车" |
| 6 大 | 抽象接口 | "6 大抽象接口：VectorStore、ToolRegistry、InputProcessor、PostProcessor、SSE 协议、SessionStore" |

---

> **使用提示**：答辩前 2 小时，通读一遍本文档。每页演讲稿建议自己朗读一遍，计时 20-30 秒，找不顺口的地方修改。关键数字写在便签上贴在电脑前。
