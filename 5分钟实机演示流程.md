# 5 分钟实机功能展示流程脚本

> **使用场景**：答辩现场，实机演示 Android 客户端 + 后端服务  
> **总时长**：5 分钟（300 秒）  
> **核心原则**：每步操作有预期结果，每步展示有 1 句技术说明，体现"工作量"和"理解深度"  
> **建议**：提前打开后端服务、Android 客户端、PPT 的实机展示页面，全部就绪后开始计时

---

## 准备工作（不计入 5 分钟，提前 2 分钟完成）

| 检查项 | 状态 | 备注 |
|---|---|---|
| 后端服务已启动（`uvicorn server.main:app --reload`） | ☐ | 确认 http://127.0.0.1:8000/health 返回 ok |
| Android 客户端已启动（真机或模拟器） | ☐ | 确认网络通畅 |
| 输入框清空，无历史会话 | ☐ | 点击新建会话或清空当前会话 |
| 手机屏幕已投屏到电脑（方便评委观看） | ☐ | 可用 scrcpy、投屏工具或镜像 |
| 后端 `data/products_ref.json` 数据正常 | ☐ | 确认 100 条商品可用 |
| 测试以下 5 个查询都正常返回（提前测一遍） | ☐ | 防现场翻车 |

**翻车预案**：如果实机演示时网络/服务异常，立刻切换到提前录制的录屏视频（建议提前准备一份 5 分钟录屏作为 backup）。

---

## 时间轴总览

| 阶段 | 时间 | 展示内容 | 工作量体现 |
|---|---|---|---|
| **开场** | 0:00-0:30 | 单件推荐：保湿眼霜，预算 250 | 流式渲染 + 商品卡片 + RAG 检索 |
| **多轮 + 排除** | 0:30-1:30 | 追问预算 + 反选排除 | 会话状态累积 + 7 层 PostProcessor |
| **对比** | 1:30-2:00 | 科颜氏 vs AHC 眼霜对比 | 两侧独立检索 + 结构化对比面板 |
| **场景组合** | 2:00-3:00 | 三亚度假组合推荐 | 模板匹配 + 穷举 + 预算校验 |
| **购物车** | 3:00-3:30 | 把推荐商品加到购物车 | 自然语言操作 + 确认机制 |
| **图搜（可选）** | 3:30-4:00 | 上传图片找相似商品 | 视觉签名 / 多模态 embedding |
| **收尾** | 4:00-5:00 | 总结工作量 + 技术理解 | 不提技术但处处是技术 |

**合计：5 分钟**

---

## 详细流程脚本

### 阶段 1：开场 — 单件推荐（0:00-0:30，30 秒）

**操作**：
1. 在 Android 客户端输入框输入：
   ```
   推荐一款保湿眼霜，预算250以内
   ```
2. 点击发送

**预期结果**：
- 屏幕出现 "正在为您搜索..."（即时 token，感知优化）
- 约 300 毫秒后，文字开始逐字出现（打字机效果，SSE 流式渲染）
- 文字内容包含：推荐语 + 推荐理由
- 下方出现商品卡片：科颜氏牛油果保湿眼霜（或类似），显示图片、名称、价格、品牌
- 卡片可点击展开详情

**展示话术**（边操作边说）：
> "我先展示一个基础推荐场景。用户说'推荐一款保湿眼霜，预算 250 以内'。系统首先发送一个即时 token 让用户看到'正在搜索'，降低等待焦虑。然后约 300 毫秒后，文字开始逐字出现——这是 SSE 流式推送，后端生成一个 token 就推一个，前端边收边渲染。"

**技术点（自然带出）**：
> "注意这个商品卡片——它来自 RAG 检索的真实结果，不是 LLM 编造的。卡片里有证据链：商品 ID、价格、品牌、检索分数。"

**工作量体现**：
- SSE 流式 API 实现（`server/api/chat.py`）
- Android 打字机效果（`ChatSseClient.kt` + `MessageComponents.kt`）
- RAG 5 阶段检索 Pipeline（`retrieval_pipeline.py`）
- 商品卡片组件（`ProductComponents.kt`）

---

### 阶段 2：多轮对话 + 反选排除（0:30-1:30，60 秒）

**操作 1：预算追加（0:30-0:50，20 秒）**
1. 在输入框继续输入：
   ```
   再便宜一点
   ```
2. 点击发送

**预期结果**：
- 系统理解"再便宜一点"是在上一轮基础上追加预算约束
- 返回更低价格的眼霜商品（如 AHC 眼霜，价格 < 上一轮最低价格）
- 商品卡片更新

**展示话术**：
> "现在看多轮对话。用户说'再便宜一点'——系统不会重新理解整个需求，而是在上一轮 session 的过滤条件上追加'预算低于上一轮最低价'。这体现了会话状态的累积机制。"

---

**操作 2：反选排除（0:50-1:30，40 秒）**
1. 在输入框输入：
   ```
   不要科颜氏
   ```
2. 点击发送

**预期结果**：
- 返回结果中不出现任何科颜氏品牌的商品
- 系统理解"不要"是排除操作，不是关键词搜索
- 如果当前结果中无科颜氏，系统会提示或正常返回

**展示话术**：
> "再看反选排除。用户说'不要科颜氏'——这个'不要'不是让 AI 理解，而是走结构化的排除过滤器。系统通过品牌别名表识别'科颜氏'，然后在 PostProcessor 阶段直接排除。这样做比让 LLM 判断更稳定、可测试。"

**技术点**：
> "这里的技术细节是：'不要科颜氏'被解析为 `exclude_brand=科颜氏`，在 7 层 PostProcessor 的 BrandFilter 中直接过滤掉。不是让 LLM 判断'这个商品是不是科颜氏'，而是程序化过滤。"

**工作量体现**：
- SessionState 多轮状态管理（`session/state.py`）
- 语义规划双层设计（`semantic_llm.py` + `semantic_rules.py`）
- 7 层 PostProcessor 过滤链（`post_process.py`）
- 品牌别名表（`brand_aliases.json`）

---

### 阶段 3：商品对比（1:30-2:00，30 秒）

**操作**：
1. 在输入框输入：
   ```
   科颜氏和AHC哪个眼霜更适合干皮
   ```
2. 点击发送

**预期结果**：
- 文字回答："我先基于当前商品库做对比..."
- 出现两个商品卡片：科颜氏眼霜 + AHC 眼霜
- 出现对比面板（Comparison Panel）：价格、容量、成分、适用场景逐一对比
- 有推荐结论（如"如果预算有限推荐 AHC，如果追求长效保湿推荐科颜氏"）

**展示话术**：
> "商品对比功能。用户说'科颜氏和 AHC 哪个眼霜更适合干皮'，系统解析出两个对比品牌，分别检索两侧，各自召回 Top 5，然后生成结构化对比面板。这里的关键设计是**两侧独立检索**，防止 Top-K 只召回一个品牌导致对比失衡。"

**技术点**：
> "对比不是简单的把两个商品放一起，而是分别构建查询'科颜氏眼霜'和'AHC眼霜'，各自走完整的 RAG Pipeline，然后合并对比。这样保证对比的公平性。"

**工作量体现**：
- CompareHandler 意图识别（`commerce_handlers.py`）
- ProductCompareTool 两侧检索（`product_compare.py`）
- 对比面板 UI（`ComparisonPanel.kt`）
- 结构化对比生成（`build_comparison_answer`）

---

### 阶段 4：场景组合推荐（2:00-3:00，60 秒）

**操作**：
1. 在输入框输入：
   ```
   下周去三亚度假，帮我搭配一套从防晒到穿搭的方案，预算1000以内
   ```
2. 点击发送

**预期结果**：
- 文字回答："为您搭配三亚度假组合方案..."
- 出现多个商品卡片：防晒霜、轻便上衣、舒适出行鞋、拍照装备等
- 商品按场景 slot 分组展示
- 总价格不超过 1000 元

**展示话术**（分两段说）：
> "场景组合是我们的特色功能。用户说'去三亚度假，帮我搭配一套'，系统匹配到'三亚旅行'模板，包含防晒、上衣、出行、拍照 4 个 slot。每个 slot 独立检索，然后穷举所有组合，按预算打分，输出最优方案。"

> "这里的技术亮点是**配置驱动**：场景模板存在 JSON 文件里，新增一个场景（比如'露营装备'）只需改配置文件，10 分钟扩展，不需要改代码。"

**技术点**：
> "组合推荐不是让 LLM 自由发挥，而是穷举所有候选组合，按预算和匹配度打分，保证结果在预算内且各 slot 都有商品。这避免了'推荐了一个组合但某个 slot 缺货'的问题。"

**工作量体现**：
- ScenarioBundleHandler 模板匹配（`commerce_handlers.py`）
- BundleOptimizer 穷举打分（`bundle_optimizer.py`）
- 场景模板 JSON 配置（`scenario_bundles.json`）
- 预算校验与组合渲染（`CartPanel.kt` / `ChatScreen.kt`）

---

### 阶段 5：对话式购物车（3:00-3:30，30 秒）

**操作**：
1. 在输入框输入：
   ```
   把刚才推荐的防晒霜加到购物车
   ```
2. 点击发送

**预期结果**：
- 系统确认："已将 XX 防晒霜加入购物车，数量 1。"
- 购物车面板展开（或角标显示数量 +1）
- 购物车显示：商品名称、价格、数量、总价

**操作 2**（可选，如果时间充裕）：
1. 输入：
   ```
   购物车有什么
   ```
2. 点击发送

**预期结果**：
- 系统返回购物车清单
- 购物车面板显示完整商品列表

**展示话术**：
> "最后展示对话式购物车。用户说'把刚才推荐的防晒霜加到购物车'，系统通过上下文引用找到上一轮推荐的商品，执行加购操作。购物车支持自然语言操作：'加购''删除''改数量''查看'，全部走结构化解析。"

**技术点**：
> "购买操作有确认机制，如果用户说'买两双'，系统不会直接执行，而是先问'确认加入购物车吗？'，防止误操作。这是电商场景的安全设计。"

**工作量体现**：
- CartHandler 意图识别（`commerce_handlers.py`）
- CartTool 操作解析（`cart.py`）
- 自然语言操作解析（`cart_operations.py`）
- 购物车 UI 面板（`CartPanel.kt`）

---

### 阶段 6：图片找货（可选，3:30-4:00，30 秒）

> **如果时间紧张，此阶段可跳过，直接收尾。**  
> **建议**：提前准备一张测试图片（如运动鞋图片），放在手机相册中。

**操作**：
1. 点击输入框旁的 "+" 或图片按钮
2. 选择预先准备的运动鞋图片
3. 发送

**预期结果**：
- 系统提示："正在按图片搜索相似商品..."
- 返回相似商品卡片（如耐克运动鞋、 Adidas 运动鞋等）
- 有相似度提示（如"相似度 0.85"）

**展示话术**：
> "图片找货功能。用户上传一张运动鞋图片，系统提取视觉特征——有 API 时走多模态 embedding，无 API 时 fallback 到 12×12 像素视觉签名。返回最相似的商品。"

**技术点**：
> "两套方案在架构中并存：优先调用豆包多模态模型获取 768 维向量，如果网络不通或没有 API Key，自动降级到本地视觉签名。这保证了项目开箱可运行。"

**工作量体现**：
- MultimodalInputProcessor 图片处理（`multimodal.py`）
- 视觉签名算法（`image_similarity.py`）
- 多模态 embedding 缓存（`visual_embedding.py`）
- 图片上传组件（`ChatScreen.kt` 图片选择器）

---

### 阶段 7：收尾总结（4:00-5:00，60 秒）

> **此阶段不再操作，面向评委总结，用 3-4 句话展示"工作量"和"技术理解"。**

**话术**：
> "以上就是实机演示。回顾刚才的 5 个场景，背后有这几个技术点：
>
> 第一，**RAG 检索 + 防幻觉**。所有商品信息来自真实检索，不是 LLM 编造，通过 3 层机制保证事实准确。
>
> 第二，**多轮状态管理**。用户的预算、排除、品牌偏好是多轮累积的，但品类切换时自动清理，防止旧条件污染新需求。
>
> 第三，**结构化优于 AI 理解**。反选、对比、购物车操作都是程序化解析，不是让 LLM 猜，这样稳定、可测试、可复现。
>
> 第四，**工程可扩展性**。6 大抽象接口、配置驱动的场景模板、即插即用的 PostProcessor，保证系统从 MVP 到生产的演进路径清晰。"

---

## 常见问题：实机演示翻车怎么办？

| 翻车场景 | 应急方案 | 备用话术 |
|---|---|---|
| 后端服务崩溃 | 立刻切换到录屏视频 | "刚才网络有点波动，我切换到提前录制的演示视频" |
| Android 客户端闪退 | 重启应用，继续下一个场景 | "客户端重启一下，我们快速继续" |
| LLM 超时无响应 | 等 3 秒直接说"这里 LLM 超时了，系统会自动降级到模板回答" | 反而能展示降级机制 |
| 商品返回不对 | 不要慌，继续下一步 | "这里返回的是 fallback 结果，说明当前条件下没有精确匹配，这也是 RAG 的预期行为" |
| 图片上传失败 | 跳过此步骤，直接总结 | "图片上传依赖当前网络环境，我们回到文字场景" |

---

## 附：5 分钟演示用录屏脚本（如无实机条件）

> 如果评委不允许实机演示，或现场环境不稳定，提前录制一段 5 分钟录屏，按以下脚本配音。

| 时间点 | 画面内容 | 配音脚本 |
|---|---|---|
| 0:00-0:30 | 打开 App，输入"推荐一款保湿眼霜，预算250以内" | "演示第一个场景：基础推荐。用户输入需求，系统 300 毫秒内返回首 token，文字逐字出现，下方展示商品卡片。" |
| 0:30-1:00 | 输入"再便宜一点" | "多轮对话：追加预算约束。系统在当前 session 上累积条件，返回更低价商品。" |
| 1:00-1:30 | 输入"不要科颜氏" | "反选排除：系统解析'不要'为排除操作，通过品牌别名表排除，不走 LLM 判断。" |
| 1:30-2:00 | 输入"科颜氏和AHC哪个眼霜更适合干皮" | "商品对比：两侧独立检索，生成结构化对比面板，避免 Top-K 只召回一个品牌。" |
| 2:00-3:00 | 输入"下周去三亚度假，帮我搭配一套，预算1000以内" | "场景组合：匹配三亚模板，4 个 slot 独立检索，穷举组合，预算内输出最优方案。" |
| 3:00-3:30 | 输入"把刚才推荐的防晒霜加到购物车" | "对话式购物车：自然语言加购，支持上下文引用，购物车面板实时更新。" |
| 3:30-4:00 | 上传图片（或跳过） | "图片找货：上传图片，系统提取视觉特征，返回相似商品。支持多模态 embedding 和视觉签名 fallback。" |
| 4:00-5:00 | 总结画面 | "以上就是全部功能演示。每个场景背后都有明确的工程实现：RAG 检索、双层语义规划、7 层过滤、会话状态管理、防幻觉三层。谢谢。" |

---

> **最后提醒**：
> 1. 演示前务必完整测试一遍所有输入，确保结果可预期
> 2. 准备一份录屏 backup，放在桌面，随时可以切换
> 3. 演示时语速稍快，但不要慌，每个操作留出 1-2 秒给评委看清结果
> 4. 如果评委追问技术细节，不要展开，说"这个在后面的技术深度部分会详细说明"
> 5. 演示结束时留 1 秒停顿，让评委看到最终画面，再切换回 PPT
